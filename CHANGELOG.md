# Changelog

Format adapted from [Keep a Changelog](https://keepachangelog.com/). Git holds the authoritative history; this file is the human-readable audit trail. The discipline is inherited from the DCCREG programme conventions; the physics is mainstream (no DCCREG theory).

## [Unreleased]

### Frozen
- **Design freeze v0.10 (temporary, tag `freeze-v0.10`) — disc 8→12 mm → C_R 960→789 pF** — temporary github snapshot of the validated v0.10 geometry + cap set (`docs/varcap-design-freeze-v0.10.md`). The C_R septum disc went 8→12 mm garolite, pinning **C_R 960→789 pF** at the r387 electrode (f₀ 637 kHz, Z₀ 316 Ω). This **lowers the single-kick reach floor (½·C_R·V²) 108→89 mJ**, **improves the M2 island-dump match η 96→99 %** (648 pF island ≈ 789 pF tank), and **widens the fire window 18.6–21 → 16.6–21 kV**. C1/C2 (r387, 16–280 pF), Cx (648 pF), Ca/Cb (309 pF) held; pump z 1.307, galvanic ceiling 1.334, ledger clean — all unchanged. **Six spark gaps placed** (SG1/SG2 return, SG3a/SG4a load, SG3b/SG4b fire — 12 mm W-Cu spheres). **Open / named TODO:** BS3/BS4 backstops (19°/49°, 0.6×strike, outer ~r350–380) + the `varcap-nodeanalysis-template-r0_10_TMD_layout.dxf` (not yet drafted), the S3 spark tier, and the S2 reach re-confirm at 789 pF. Frozen solvers (`shuttle_core.py`, `reference/doubler_core.py`) + `index.html` byte-identical (empty diff asserted); documentation + DXF only. Temporary freeze — **not merged**.

### Investigated
- **FIRING-GEOMETRY — clock the commutator from the DXF stations, then sweep station/radius/ball → `STATIONS-CONFIRMED + HINTS` (branch `firing-geometry` off `netlist-gaps-rederive`)** — resolves the ⚠ commutation-timing flag by reading the **DXF 00-FIRING-STATIONS** (SG1 3.0°, SG3a 7.2°, SG3b 16.05°, BS3 19°, SG2 33°, SG4a 37.2°, SG4b 46.05°, BS4 49° — from `shuttle_event_angles.csv` + the DXF) instead of an assumed window, then explores a station/radius/ball perturbation lookup. New **`sim/firing_geometry.py`** (DXF-station bind + the electrode-overlap-window model + the lookup sweep; reuses the frozen anchor + `commutator_real_core`), `firing_lookup.csv` (1260 combinations, A1–A5), `firing_radius_trade.csv`, `firing_geometry.png`, `sim/firing-geometry-findings.md`. **Baseline:** at the real stations the conduction window IS the electrode-overlap window `Δθ_overlap=(d_ball+2·g_lat)/r`; with the established **12 mm W-Cu spheres**, r=387 mm, 3000 rpm → overlap **2.07°=115 µs** vs the fire need (t_strike+t½+t_conduction≈**4.3 µs**, t½=island Lx 2.2 µs) → **fit margin 111 µs**, so **η_real = 0.703** holds (the timing flag is **resolved by geometry, not sign-off**; not `WINDOW-LIMITED`). **The headline design finding:** the binding constraint is **NOT** the window but **CROSS-FIRE (A2)** at the tightest station pair **SG3b→BS3 = 2.95°** — of the 462 assertion failures in the sweep, **all are A2; A1 (window-fits) never fails even at 30 000 rpm**. **The radius criticism, resolved:** the radius is *not* critical for fitting the resonant transfer (vast time margin) — it is the lever on cross-fire, and **larger r ⇒ narrower overlap ⇒ more margin** (opposite to a too-narrow worry): at r=250 mm a 12 mm ball already cross-fires (margin −0.26°), at r=387 mm margin +0.88°, at r=491 mm +1.32° (and admits a 23 mm ball). **Design hints:** η_real is flat (~0.70) across the viable region, so geometry moves *margin*, not efficiency; the DXF stations are near-optimal (no shift needed/beats them); keep the BS3/SG3b balls at the **outer band (r ≥ ~300 mm, prefer 387–491)** with `d_ball ≤` the per-radius cap; the inner band (r ≤ 250 mm at the 12 mm ball) is a cross-fire wall. Conservation guard still closes (9.4×10⁻¹²) + trips at the DXF-clocked operating point. Frozen `doubler_core`/`shuttle_core`/`index.html` byte-identical. **Verdict `STATIONS-CONFIRMED + HINTS`**; **not merged** (design-exploration feeding the gap-placement spec).
- **NETLIST-CORRECTION — the real 8-gap topology from TMD's schematic + the re-derive (branch `netlist-gaps-rederive` off `commutator-real`)** — COMMUTATOR-REAL flagged the KiCad `.net` export as a stale/partial capture (37 comps, only `SG3a1`/`SG4a1` of the eight commutation gaps). TMD supplied the live **`DCCREG_Turbine_circuit.kicad_sch`** (43 comps, **all 8 gaps**: SG1, SG2, SG3a1, SG3b1, SG4a1, SG4b1, BS3, BS4), now the in-repo source of record. The schematic has **no net labels** (pure wire/junction geometry), so new **`sim/sch_to_netlist.py`** reconstructs connectivity geometrically with a **pin-exact transform — 86/86 pins land on wire endpoints** (validated; reproduces the old export's non-gap nets). New **`sim/netlist_gaps.py`** (the 8-gap consistency check + the rectifier-role map → **8/8 present & consistent**) and **`sim/rederive_from_gaps.py`** (regenerates `topology_edge_list.csv` from the schematic — 43 comps — and re-runs the verdict chain). **The real gap roles:** SG3a/SG4a load (rail→island), SG3b/SG4b fire (island→cross-coupled bank), BS3/BS4 the FE backstops **in parallel** with the fire gaps, SG1/SG2 fire the AR/BR banks **into the resonator tank**. **Two corrections vs the earlier lumped-`shuttle_core` assumption, neither moving a verdict:** (1) SG1/SG2 do **not** return to ground (the machine is floating/differential) — they fire the banks into the tank; the COMMUTATOR-REAL rectifier modeled the *holdoff* on banks 2,3 (≤ V_strike), which SG1/SG2 + the island-fire gaps provide regardless of the fired charge's destination; (2) BS3/BS4 sit in parallel with SG3b/SG4b, not on a bank-to-ground leg. **Re-derive result:** the real topology provides exactly the V_strike holdoff on the transfer banks that COMMUTATOR-REAL assumed → **`BRIGADE-RECOVERABLE` re-confirmed on the real topology** (direct-limit α→0 reproduces frozen z 1.334/η 0.386; the diode v≤0 stand-in z 1.573/η 0.404 is **replaced** by the real spark-gap holdoff → α_max 0.807, z 2.478, η_gross 0.709 → **η_real ≈ 0.70**, keep Ca/Cb). Also **`docs/kicad/gap-topology-of-record.md`** (the documented real topology), **`docs/kicad/symbols/dccreg_gaps.kicad_sym`** (optional self-documenting Spark_Gap/FE_Gap symbols + SG*/BS* naming convention), `sim/netlist-correction-findings.md`. The stale provisional `gap-completion-spec.md` (which had wrongly assumed SG1/SG2→ground) removed. Frozen `doubler_core`/`shuttle_core`/`index.html` untouched; the gaps are consumed from the schematic of record. **Not merged.**
- **COMMUTATOR-REAL — re-derive the brigade price with spark gaps + field emission (not diodes) → `BRIGADE-RECOVERABLE` (keep Ca/Cb; branch `commutator-real` off `doubler-resonant`)** — the `doubler-resonant` η 0.404 used **rail-return diodes** as the rectifier (a hard clamp at v≤0). **The machine has no diodes:** the commutation is **spark gaps that hold off to V_strike** + a **Fowler-Nordheim field-emission backstop** (a soft bleed). The diode-at-0 was a **stand-in artifact** that pinned α_max at 0.28; the real gap holds off to 20 kV, giving the over-transfer far more headroom. New **`reference/commutator_real_core.py`** (the V_strike sparking gaps + the FN FE leg + the independent guard; reuses `doubler_resonant_core`'s over-transfer cycle and `island_resonant_core`'s guard, both unmodified), **`sim/commutator_real.py`** (driver + verdict), `commutator_real.csv`, `commutator_real.png`, `sim/commutator-real-findings.md`. Frozen `doubler_core`/`shuttle_core`/`index.html` **byte-identical**. **The model `[OC]/[IR]`:** replace the diode threshold (gap v≤0) with the spark-gap threshold (gap voltage ≤ V_strike); α_max = the swing at which the first gap reaches V_strike (it fires, arcs E_arc=V_arc·Q, self-quenches). The FE backstop (FN J∝E²·exp(−B/E), onset 0.6·V_strike=12 kV, the validated BS3/BS4) bleeds charge **softly** along the swing+dwell — a real, accounted loss, not a clamp. **Direct-limit anchor (gate):** α→0 reproduces frozen **z 1.3340 / η 0.3860**; **diode-limit cross-check:** V_strike→0 reproduces the `doubler-resonant` clamp (z 1.573 / η 0.404) — the only change is the realistic holdoff. **THE RESULT:** the gap holdoff lifts **α_max 0.28 → 0.81** and **η_gross 0.404 → 0.709** at the established V_strike=20 kV (it recovers ~31 of the 61 efficiency points the diode wrote off). The real price is now a **small, quantified FE-bleed + arc BUDGET** (recovered_net = recovered_gross − E_FE − E_arc): the **arc is negligible** (~0.05 mJ/cycle); the **FE bleed** stays small for a **designed backstop** (a controlled low-leakage safety clamp, not a conduction path) — at the central estimate (**30 µA, commutating within the SG window**) **η_real = 0.697**; across the plausible envelope **η_real ≈ 0.65–0.70**. Only an implausibly leaky leg (≥100 µA **and** a full-sector 1.67 ms dwell — tens of watts at 20 kV) degrades to `BOUNDED`/`PRICE-CONFIRMED`. **Conservation:** ring guard closes **9.4×10⁻¹²** AND trips +5 % R; the FE bleed is a **real perturbable** loss (+5 % leakage moves E_FE) — no free recovery. **DECISION (brief §6):** **KEEP the Ca/Cb brigade inductors** (Cx/Lx already kept); the synthesizer's η/anchor should be set to the **real commutator value ~0.70**, NOT 0.404 and NOT 0.999 — pending TMD's FE-leg spec (leakage×dwell). **Netlist-completion flag:** the KiCad export carries only `SG3a1`/`SG4a1` (drawn as `SolderJumper_2_Open` = gaps); **missing: SG3b, SG4b, the FE/BS3/BS4 legs, and the doubler gaps SG1/SG2** — recommend **custom spark-gap + FE-gap KiCad symbols** + a naming convention (`SG*` sparking, `FE*`/`BS*` field-emission) so the parser classifies gaps vs caps. **Verdict `BRIGADE-RECOVERABLE`** — the diode 0.404 was the artifact; the deliberate V_strike holdoff (gaps, not silicon) gives the over-transfer the room a junction denies, and the bucket-brigade tax is **mostly recoverable** after all. Frozen solvers untouched; **not merged** (resolves the Ca/Cb keep/drop; the synthesizer/topology + the KiCad gaps follow).
- **DOUBLER-RESONANT — does z survive resonant equalization, or is the loss the price of the ratchet? → `Z-RETUNED` (the price is real; branch `doubler-resonant` off `resonant-brigade`)** — the gate on the resonant-brigade η (0.999 was an **upper bound**, conditional on z surviving). This block re-derives the Bennet doubler **with the half-cycle LC + diode resonant equalization in place** and answers what happens to **z = 1.334**. New **`reference/doubler_resonant_core.py`** (the resonant Bennet cycle + the rectification clamp + the independent guard; **regression-locked** to the frozen `doubler_core`), **`sim/doubler_resonant.py`** (driver + verdict), `doubler_resonant.csv`, `doubler_resonant.png`, `sim/doubler-resonant-findings.md`. Frozen `doubler_core`/`shuttle_core`/`index.html` **byte-identical** (the resonant doubler is a **new** solver; `island_resonant_core` reused **unmodified**). **The model `[OC]`:** the direct equalization shares charge **to the mean** (the two-cap tax); done resonantly the inductor current is maximal *at* the mean, so the lossless disconnect is only at the next **current-zero**, leaving the caps **over-transferred** — parameterised `V_res = V_direct + α·(V_direct − V_pre)`, α = √(1 − π/Q) (α=0 the direct/frozen anchor, α=1 the lossless swap; the brigade ring Q≈1909 → α≈0.999). **Direct-limit regression (the gate):** α→0 reproduces frozen **z 1.3340 / η 0.3860** bit-close (not `MODEL-FAIL`) — the resonant model **is the same doubler**, only the transfer mechanism changed. **THE RESULT:** the naive unconstrained over-transfer would give **z 3.0 / η 0.999** (= the brigade upper bound) **but that state is diode-INVALID** (100 % of held states forward-bias the rail-return diodes). **Enforcing rectification** (a held state at current-zero needs ALL diodes blocking) **clamps the over-transfer at α_max ≈ 0.28 — independent of Q** — because the over-transfer drives the inner nodes (2,3) positive and the **rail-return diodes D1(2→0)/D2(3→0) re-conduct**. So z is **re-tuned to 1.573** (the ladder **re-settles**, does NOT collapse to 1; rectification stays one-way) but **η moves only 0.386 → 0.404** (+1.8 pts), **far below the 0.999 upper bound**. **The bucket-brigade tax is ~97 % INTRINSIC to the ratchet:** the rail-return diodes that make the Bennet pump *pump* are exactly what forbid the over-transfer that would recover the tax — and α_max is **structural** (v2,v3→0 is topology, not a tunable ratio). **Mechanism (brief §3):** ladder re-settles to a new stable balance; **rectification intact (the price is the STATE, not lost one-way)** over 200 steady half-cycles; over-transfer leverage raises z but the clamp caps η; no re-tune headroom. **Conservation:** ring guard (reused `island_resonant_core`) closes **9.4×10⁻¹²** AND **trips +5 % R**; the over-transfer **feeds back** (no free-sink — +5 % α moves the unclamped z 1.868→1.908). **TMD's "there is a price" is CONFIRMED** — the block was built to falsify it and instead falsifies the free-recovery story. **Implication:** the 2 brigade inductors **come OFF** the doubler core; the η headline **reverts** from 0.999 to ~0.40, and the real, sized efficiency win stays the **downstream island** (RESONANT-ISLAND, ~31 % combined-tax drop — a true sink, unconditional). `design_synth` z-band/η₀ **unchanged** (the adopted machine stays the **direct** doubler; the resonant doubler is not adopted). **The deepest result in the arc: not all tax is recoverable, because some of it is what makes the pump pump** — the doubler-core efficiency floor (~0.386, at best ~0.40 with a diode-bounded resonance) is set by the ratchet mechanism itself. **Verdict `Z-RETUNED` (the price is real)**; frozen solvers untouched; **not merged** (resolves that the brigade fix is not real on the core — informational; the island fix stands).
- **RESONANT-BRIGADE — localize the doubler tax, then resonate the dominant transfers → `RESONANT-BRIGADE-MODELED` (CONDITIONAL on z; branch `resonant-brigade` off `resonant-island`)** — the resonant-island decomposition left the **doubler Ca/Cb bucket-brigade C-C tax (9.79 mJ/fire = 69%)** untouched; this block **localizes** where inside it that tax lives, then **resonates only the dominant transfers** with the validated `island_resonant_core`. **Phase 1 — `sim/brigade_tax_localize.py`** (consumer analysis on the **frozen** `doubler_core` trajectory; never an edit): the per-phase equalization tax `Etax = U_int(diodes off, const-Q) − U_post`, anchored at the **15 kV operating point** (same anchor as `energy_balance.csv`: W_mech 15.941 mJ/fire, η 0.386). **Sum-check PASS: Σ Etax = 19.577 mJ/cycle = 2 fires × 9.789 mJ/fire** (the published "9.79" is the **per-fire** figure; a cycle has two fires — units reconciled), which also **cross-validates the assumed 2-phase transfer sequence** (phase A then B — flagged for TMD). **Localization:** the tax is **CONCENTRATED in 2 switched phase transfers** — **phase A 11.19 mJ/cycle (57%)** + **phase B 8.39 mJ/cycle (43%)**, both with effective transfer C **≈ 68.6 pF** (a topology constant of Ca/Cb/Cpar) — **NOT smeared** across the 12 C_AR/C_BR (so resonance is the right lever, not architectural; rules out `TAX-SMEARED`). **Phase 2 — `sim/resonant_brigade.py`** (reuses `island_resonant_core` **unmodified**): each dominant transfer gets a series brigade inductor (Lx 1 mH) and rings half-cycle into the rail. **Per transfer:** worth-it (Q floor) **PASS** at **Q≈1909 (R=2Ω)**, the **`island_resonant_core` guard closes (resid ~1e-12) AND trips under +5% R**; **aggregate ledger closes exactly** (Σ recovered 19.545 = Σ(tax 19.577 − ring loss 0.032), residual 0). **Multi-resonant clocking:** both **t½ ≈ 0.82 µs** (shared C_eff → identical) **≪ the 5°@3000 rpm = 278 µs** window → **co-exist trivially at one rpm** (not `TIMING-COUPLED-INFEASIBLE`). **Arc floor:** E_arc = V_arc·Q_transferred ≈ **0.046–0.116 mJ/cycle** (V_arc 20–50 V, `shuttle_core` corners) → the fully-resonated η ceiling is **~0.997, not 1**. **Recovered-η-vs-N curve:** η **0.386 → 0.736 (N=1, phase A) → 0.999 (N=2)**, robust across the Q band (**0.95 even at Q≈38 / R=100Ω**) — recommended set is **both inductors (N=2)**. **⚠ THE LOAD-BEARING CAVEAT (do not headline the η number):** unlike the downstream island (a pure sink dump — resonating it is free), **the brigade equalization IS the Bennet pump mechanism** — the post-equalization node voltages are the initial condition for the next stroke, so a loss-free LC ring leaves the caps in a different (over-transferred) state and **can alter z = 1.334**. Confirming z survives needs **re-deriving the doubler with LC equalization in place**, and `doubler_core` is **frozen** this block — so **the recovered-η curve is an UPPER BOUND, conditional on a doubler re-sim**. The localization, worth-it, guard, clocking and arc floor are **unconditional**; the η headline is **not**. **Integration:** `design_synth` **I10** gains a brigade multi-resonant clocking sub-check (both t½ co-exist with the island t½ at the design rpm); `brigadeL_mH` enters `ESTABLISHED`; **canary re-confirmed** (anchor still **z 1.3336, η 0.3863, 983 mm, feasible**; binding I10). Deliverables `sim/brigade_tax_localize.py`, `brigade_tax_localization.csv`, `sim/resonant_brigade.py`, `resonant_brigade.csv`, `resonant_brigade.png`, `sim/resonant-brigade-findings.md`. Frozen `shuttle_core`/`doubler_core`/`index.html` **byte-identical** (empty-diff asserted). **Verdict `RESONANT-BRIGADE-MODELED`** (the tax localizes, the dominant transfers resonate, guard+clocking+arc-floor all hold) **— with the z-preservation caveat as the dominant open item**. **Re-opens the topology** (2 brigade inductors for TMD to draw into the KiCad netlist of record); **not merged** (merges on TMD's sign-off of the transfer sequence, the inductor set, **and** a doubler re-sim confirming z).
- **RESONANT-ISLAND — the series-LC island transfer (the efficiency fix) → `RESONANT-TRANSFER-MODELED` (branch `resonant-island` off `design-synth`)** — TMD added a **series inductor to each island variable cap** (Lx3 in series with Cx3 → BR bank; Lx4 with Cx4 → AR bank; confirmed from the KiCad netlist `DCCREG_Turbine_circuit.net`, **37 components**). This converts the island charge transfer from a **direct dump** (loses ½C·ΔV² irrespective of R — the two-capacitor paradox) into a **half-cycle LC resonant transfer** (loses only the rings resistive dissipation → 0 as Q rises). New **`reference/island_resonant_core.py`** (the LC model: C_eff/Z/t½/i_pk/Q closed-form + a series-RLC RK4 **`[SOLVER]`** transient, authoritative), **`sim/resonant_island.py`** (the analysis driver), `sim/resonant-island-findings.md`, `resonant_island.csv`, `resonant_island.png`, `docs/kicad/DCCREG_Turbine_circuit.net`+`.dxf` (the new topology of record), `topology_edge_list.csv` (regenerated **37 components / 24 nodes**), `sim/design_synth.py` (uses the model; I10 + Lx). Frozen `shuttle_core`/`doubler_core` byte-identical (the resonant transfer is a **new** model; the frozen direct shuttle stays the baseline). **Assumed sequence (flagged for TMD sign-off):** SG3a charges Cx3 from the rail; Cx3 rings via Lx3 into the BR bank in a half-cycle, self-quench at current-zero; Cx4/Lx4 mirror. **Conservation closes (4×10⁻¹³) AND the +5% R trip fires (4.9%)** — the recovered tax is redistribution, not invention (Rule 6.1, not an identity). **Honest model note:** the integrated `[SOLVER]` loss runs **~2× the briefs `(π/2Q)` closed form** (the ring dissipation is ≈(π/Q)·E_2cap); the integral governs. **η + the load-bearing DECOMPOSITION:** audited **island transfer tax 4.41 mJ/fire (31%)** vs **doubler Ca/Cb bucket-brigade C-C tax 9.79 mJ/cycle (69%, UNTOUCHED by Lx)**; over the Q sweep the resonant transfer recovers **f_rec** of the island tax → at a usable **Q≈729 (R=2Ω)** recovers **4.39 mJ/fire** → the combined tax drops **31%** — a **real, sized gain**, but **the doubler bucket-brigade (69%) is untouched**, so **do NOT headline a total-η win that is only the islands share — the next inductors belong on Ca/Cb.** **Lx constraints:** feasible 0.01 mH–1 H; **t½ ≪ the 5°@3000rpm (278 µs) conduction window** at any feasible Lx → clean **self-quench at the first current-zero** (no chop/re-strike; the exact t½=window match would need ~17 H, impractical); i_pk sets the lower Lx bound (1 µH→108 A over). **I10** updated with the 3 resonant sub-checks (timing/current/voltage); **canary re-confirmed** (anchor *with* the resonant island still **z 1.3336, η 0.3863, ~983 mm, feasible**; binding still I10 — the Lx is a transfer-efficiency lever, doesnt change z/η_doubler). **Verdict `RESONANT-TRANSFER-MODELED`** — the fix is real and sized; the decomposition points the next fix at Ca/Cb. **Re-opens the locked r0.15 topology** by design (the cost of the win); frozen solvers untouched; **not merged** (a topology change — merges only on TMDs sign-off of the sequence *and* the gain).
- **DESIGN-SYNTH — a constrained dimension-chooser (free dimensions, invariant rules) → `SYNTH-FEASIBLE` (branch `design-synth` off `torque-sim`)** — the standing bridge between *intent* and *geometry*: Claude Code may choose **every** dimension (plate radii, gaps, cap values, rpm, staging, motor geometry), but each candidate is checked against a fixed battery of **invariants** — the validated derivations + rules-of-thumb + conservation — so flexibility can only move within the feasible region the rules define; it can never quietly override a derivation. New **`sim/design_synth.py`**, `sim/design-synth-findings.md`, `synth_design.csv`, `synth_compliance.csv`, `synth_feasible.png`. Frozen empty-diff asserted; **not merged** (synthesized designs feed the DXF/deck only on TMD approval). **Principle realized:** the synthesizer **calls** the frozen solvers (`doubler_core` z/η, frozen `shuttle_core` W_coll) per candidate — never re-derives/fits/overrides — and z/η being **scale-free** (ratios only) are cached by the normalized ratio tuple. **The invariant battery (I1–I10), reconstructed from the campaign's established results** (no separate numbered-rules doc exists in-repo): I1 conservation(real, +5% trip) · I2 frozen-solver authority · I3 scale-free z∈[1.20,1.45] · I4 insulate-first(gap V_bd, septum, split-coil) · I5 tax/staging(η≥0.15) · I6 parasitic floor(C_par≥20 pF) · I7 motor matched(out≤net, f_res=PRF, f₀-spectator) · I8 DC-trapped tank · I9 mechanical(rim<200 m/s, supercritical, vac≤10 Pa) · I10 shuttle integrity(strike<21 kV ceiling). **Checks:** frozen solvers called per candidate; **regression anchor REPRODUCES** (established ~1000 mm/15 kV → C1_max **280 pF** exact, z **1.334**, η **0.386**, feasible on all 10, least-slack I10); **I1 conservation closes (resid ≤1.3×10⁻¹⁶) AND the +5% self-test trips** (non-tautological — the discriminator is the trip, not the magnitude, as in torque-sim). **Synthesized designs (Claude Code chose; the rules held), each with a DISTINCT binding constraint found by perturbing the objective toward improvement:** **min-diameter** → dia **500 mm** (z 1.272, η 0.441) **bound by I3** (shrinking to dia 400 collapses the modulation, z<1.20); **max-η** → dia 700 mm, **η 0.526 bound by I3** (higher η ⇒ lower modulation ⇒ z hits the 1.20 floor); **max-rpm** → **6000 rpm** (rim 157 m/s) **bound by I9** (9000 rpm would exceed the 200 m/s rim limit). **Infeasible-goal probe** (demand dia≤300 mm @15 kV) → **`SYNTH-INFEASIBLE`** (0/6 feasible) **with the named blocker I3** — at r_out 150 the C_max (62 pF) nears the 20 pF stray floor, the modulation collapses, **z→1.000 (no pumping)**; the goal is impossible without relaxing a stated rule (higher-z family, smaller stray, or a lower z) and the tool says which. **Verdict `SYNTH-FEASIBLE`** — the flexibility lives in the dimensions, the discipline in the invariants, and the frozen solvers keep the two from trading places; the **binding constraint** per objective is the actionable output (I3 for size/efficiency, I9 for speed). *(Base-branch note: the brief's `design-optimize` base does not exist in-repo; branched off `torque-sim` and reconstructed the rules — the synthesizer drops onto a formal rules block unchanged if one is built first.)* Frozen empty-diff; **not merged**.
- **TORQUE-SIM — the torque-resolved angular machine on validated profiles → `GUARD-CLOSES + SELF-SPIN-INDETERMINATE` (branch `torque-sim` off `geom-validate`)** — the simulation the whole arc built toward: with `PROFILES-VALIDATED` (co-registered analytic A(θ), C1 max at electrical-0, fire stations exact), time-step the rotor over θ, compute the **instantaneous torques** from the geometry, and build the **genuinely independent conservation guard** — retiring the **1.2×10⁻¹⁶ identity** the energy-bookkeeping full-sim could not escape. New **`sim/torque_sim.py`**, `sim/torque-sim-findings.md`, `torque_partition.csv`, `torque_phase.csv`, `torque_phase.png`. Frozen solvers read-only (empty-diff asserted); the shuttle re-derive **runs** the frozen module. **No new energy.** **The two full-sim defects fixed for the dominant terms:** (1) **independent guard, for real** — `E_belt_in = ∮[T_varicap + T_shuttle + T_drag]·dθ` (a θ-integral of instantaneous torques, the SOURCE) vs **separately accumulated** loss models (the doubler C-C tax from equalization, reach `η_fire·E_fire`, transfer loss, governor-shed, drag — the DEST, **not** derived from the torque integrals); (2) **output as a computed torque** `T_cem = ½i²·dL/dθ`, not `P_motor − losses`. **Gate 0:** z **1.334**, η **0.386** (frozen doubler), **W_mech 15.941 mJ reconstructed as ∮T_varicap·dθ** (varicap gap back-solved g≈6.99 mm) and **W_coll 11.078 mJ as ∮T_shuttle·dθ** (single-face). **Stage B headline:** the guard closes (E_belt_in = E_diss = 28.874 mJ/cycle, resid **9.6×10⁻¹⁶**) AND the **mandatory +5% non-tautology self-test TRIPS** every torque term (varicap → **2.7%**, shuttle → **1.9%** — moving the residual ~3 orders) — which the r0.2 identity, by construction, **could not do**. *The discriminator is the trip test, not the baseline magnitude* (the baseline is tighter than the brief's nominal ~1e-6 because the varicap+shuttle energies are analytic, not stiff-ODE; the methodological point — **a guard that can fail** — is achieved). **Stage C:** `T_cem = ½i²dL/dθ` with switched-reluctance phasing (energised on rising-L so the average is net-motoring, not zero); pump-limited I_peak ~105 mA, **f₀ spectator → reach holds 14.95 kV**, island fires **20 kV < 21 kV**; **computed output torque 1.67–6.70 mN·m** across the `[IR]` L(θ) band; the guard **still closes** (9.6×10⁻¹⁶) with the Cem leg. **BALANCE:** output 1.67–6.70 vs stator drag **1.68 mN·m** → margin **−0.01…+5.0** → **the band straddles zero**. **Stage D:** four-destination partition (storage 88.76 mJ / computed output ~1.4 mJ / dissipation 28.87 mJ; tax 9.79, reach 12.64, transfer 0.19, gov-shed 4.41, drag 1.85) + **the angular record** (the long-pending phase-by-phase fire sequence on the **simulated** station angles: SG3a 7.2° load C1 217 pF → SG3b 16.05° fire C_fire 64 pF, SG4a 37.2°, SG4b 46.05°). **Stage E:** the geometric (varicap+shuttle) guard is rock-solid across every sweep (~10⁻¹⁶); the only `[IR]` lever is the Cem L(θ) depth (→ the output band). **Verdict `GUARD-CLOSES + SELF-SPIN-INDETERMINATE`** (the expected landing): the energy guard is **finally real** for the dominant terms (closes + can fail), the output is a **computed reluctance torque**, and the self-spin **sign** is gated entirely on the **concentric-pole drawing** (the `[IR]` L(θ) band straddles the drag) — a bounded drafting task, not a modelling unknown. Robust/geometric: pump+shuttle accounting, 15 kV reach under the Cem spectator, island under ceiling, drawn fire clock. Frozen empty-diff; **not merged**.
- **GEOM-VALIDATE — island reconcile + A(θ) registration (the gate before the torque sim) → `PROFILES-VALIDATED` (after 2 gate-forced corrections) (branch `geom-validate` off `geom-extract`)** — the last check before the `geom-extract` profiles become load-bearing for the torque-resolved sim. New **`sim/geom_validate.py`**, `sim/geom-validate-findings.md`, **re-emitted `geom_profiles.csv`** (analytic, registration-correct), `dxf_flags.md` updated. DXF + frozen solvers read-only (empty-diff asserted); the conditional re-derive **runs** the frozen shuttle, does not edit it. **PART A — island reconcile (decided by the AXIAL section, not back-inferred from C):** the drawn Z-stack cross-section shows the island bar (ND7, Z[1158.2,1162.2]) with **ONE pickup face** (ND3, above, across the **4.0 mm** gap) and **nothing below** → the island is **SINGLE-FACE**, not two-face/differential. The brief's premise ("648 = 2×329 single-face-air → halve to 324") is **retired on two counts**: single-face confirmed (no ×2) AND the gap is mica-loaded (not 4 mm air, the drift-check error already corrected). Single-face Cx_max ≈ **421–648 pF** (drawn bars .. solid-area; the model's 648 is the optimistic TOP), representative **~471 pF (~27 % lower)**. Frozen-shuttle re-derive across the band: **W_coll −0…−15 %, E_fire −0…−11 %** (−11 %/−8 % at ~471 pF); island still **fires at 20 kV < 21 kV ceiling throughout** → **`ISLAND-SINGLE-FACE`**, full-sim **qualitatively robust** (pumps/holds/under-ceiling), shuttle term carries a bounded ≤15 % downward revision; the accept-single-face-vs-redesign-to-two-face call is **TMD's** (flagged). **PART B — A(θ) registration (the catch the hatch check couldn't make):** the **analytic** annular-sector aligned overlap (6×30° over r95–387, engine-independent) is **221 080 mm²**, but the `geom-extract` **swept** A_max was **83 480 mm² — 38 %** → a real **`A-MAGNITUDE-DRIFT`** (the "hatch check 0.00 %" only proved engine self-consistency against a 3 mm axial Z-strip, exactly as the brief warned). **Root cause diagnosed + fixed in the engine:** the spin centre was the wedge-mass **centroid** (off-axis ~100 mm) and the 600 mm clustering **split a part-view** → partial/mis-registered overlap; the engine now **circle-fits the spin axis** + **merges groups by spin centre**, and the **exact analytic A(θ)** is adopted (regular sectors) and re-emitted. **C1 rotor & stator share the same 6-fold phase → aligned at θ=0 = electrical-0** ("true C1" datum); **C2 interleaved → anti-phase by design** (C2 min at θ=0, max at 30 — correct doubler, not an error). **Phase sane:** load SG3a (7.2°) at the island plateau / C1 76 % of max; fire SG3b (16.05°) mid-collapse C_fire≈70 pF / C1 46 % → **`REGISTRATION-CONFIRMED`** after the fix. **Verdict `PROFILES-VALIDATED`** — island resolved + registration confirmed → the torque sim consumes the **corrected** `geom_profiles.csv` (dC/dθ and the fire clock co-registered, the `½V²·dC/dθ` term sound); the gate **earned its place** by catching the 2.6× magnitude bug and the single-face island. *(Shared-engine note: `geom_extract.py` carries the spin-centre fix + a softened C1/C2 endpoint — the absolute C_max is gated on the axial Z-gap, not in the radial view, so informational like the fixed-cap areas; the A(θ) **shape** is validated; `geom_extract` still returns `GEOMETRY-CONSISTENT`; `geom_validate` is the authoritative analytic profile emitter.)* Frozen/DXF read-only; **not merged**.
- **GEOM-EXTRACT — r0.15 DXF → overlap(θ)/gap profiles + fire stations → `GEOMETRY-CONSISTENT` (branch `geom-extract` off `topology-recon-r0_13`)** — a **read-only `ezdxf` parser** that turns the fully-dimensioned r0.15 drawing into the **angle-profiles the torque-resolved sim consumes**: per part it emits the geometric **overlap-vs-angle `A(θ)`** + the **gap `g`** + the **fire-station angles**, validated against the drawn hatches and checked against the model's assumed values. **Purely geometric by design** (emits `A(θ)`/g/stations/dims, NOT C/L — the sim applies ε/μ/fringing; a nominal C is computed for the drift check only). New **`sim/geom_extract.py`**, `sim/geom-extract-findings.md`, `geom_profiles.csv` (θ-sampled `A(θ)` for C1/C2/Cx3/Cx4), `geom_stations.csv`, `geom_fixed.csv`, `geom_profiles.png`. DXF + frozen read-only (empty-diff asserted). **Coordinate reality resolved:** r0.15 lays each part 1:1 in its own **translated view-frame** (not concentric about the datum), datum/12-sector-grid/ref-radii as a (0,0) overlay — "true-scale selection" = pick a part's own-view instance (extent ∈[R25,R500] about its spin centre), reject the +y schematic glyphs + the r≈5009 replica; **stator** wedges are closed polylines but **rotor** wedges are **open arc+radial strokes** (stitched into closed loops). **Parser-correctness PASS:** grid area-engine re-integrates each drawn hatch to **0.0–0.6 %**, analytic 6×30° annulus to **3.2 %** → not `PARSER-INVALID`. **CONSISTENT:** **C1/C2** plate sweep `A(θ)` **0→83 480 mm²** (model C_min 16 pF = **fringe floor**, not geometric; implied axial gap **2.64 mm**); **all four freeze fire-stations EXACT** (drawn **SG3a 7.2 / SG3b 16.1 / SG4a 37.2 / SG4b 46.0°** vs **7.2/16.05/37.2/46.05**, <0.1°; SGs drawn 6-fold, station = angle mod 60°); **Cx3/Cx4 reconcile** under the **design mica gap** (3.0 mm + 0.3 mm mica/face, ε_r 5.4 → ~2.5 mm eff → ~520–540 pF vs model 648). **Correction (superseding the first-pass `GEOMETRY-DRIFTS`):** the Cx "factor 2 / two-face island" was a **drift-check error in this parser** (it modelled the 4.0 mm drawn *envelope* as **air**), **not** a hardware drift and **not** a full-circle-vs-sector mistake (the parser already measured the *sectored* 148 k mm² overlap, ~0.4 of the full annulus — a full-circle grab would have *overshot* ~820 pF). Calibration confirms the model's method exactly: **G1 88 pF = 6-sector r75–232 / 7.6 mm air**, and **648 pF = 6-sector r75–350 / 2.51 mm-eff**; the factor 2 decomposes exactly (×1.97) into dielectric/gap (×1.59) + bar-fill (×1.24). **One soft residual (flagged, deferred to the sim — not a drift):** the model's 648 pF idealizes the island as a **solid** 6-sector plateau; the drawn **discrete bars** realize **~81 %** of that area → real Cx_max ≈ **525 pF (~19 % below 648)**, closed by inter-bar **fringing** (the sim's job) or a small model trim. **Scope / drawing punch-list (NOT drifts):** Ca/Cb/C_R electrode **area** (`ND*-…-ELECTRODE` layers carry only schematic glyphs; gaps 1.0/12.0 mm confirmed) + **L_A/L_B Cem pole sweep** (motor is a distribution schematic, not concentric poles) — need dedicated part-view/attributed layers next rev. **Bottom line:** the S5–full-sim results riding on **station timing, plate geometry, and the island cap** are on hardware-matching numbers (island within the sim's fringing tolerance). Frozen/DXF read-only; **not merged**.
- **Topology recon r0.2 — net-for-net vs the committed r0.15 EE schematic → `TOPOLOGY-CONFIRMED` (branch `topology-recon-r0_13` off `main`)** — the **r0.15 EE schematic is now committed** (`docs/varcap-nodeanalysis-template-r0.15_TMD_layout.dxf`), so the net-for-net wiring check r0.1/r0.2 couldn't run (no schematic in the repo → `TOPOLOGY-INCOMPLETE`) finally proceeds. Rewrote **`sim/topology_recon.py`** r0.2 (the r0.15 **net-tracer**: schematic parse + wire-graph by endpoint coincidence + symmetry/mislabel audit + the `[?]`/motor/split-resonator confirmation), `sim/topology-recon-findings.md` r0.2, `topology_edge_list.csv` (re-stamped **DXF-sourced r0.15** — the manifest of record), `dxf_flags.md`. DXF + frozen read-only (empty-diff asserted). **Completeness RESOLVED:** the schematic carries the 00-CIRCUIT-SCHEMATIC + SCHEMATIC layers (drawn wiring), the motor (MECH-CEMS/COIL-CEMS/quadricores + 10 INSERT arrays), the split-resonator nodes (ND9/10), and **all 22 node labels** — the ND9/10 + 24-component motor that r0.1/r0.2 flagged as undrawn are now present. **Checks:** Stage B symmetry **10/11 pairs mirror exactly**, the **one asymmetry ND7↔ND8** (ND8 ~157 off its mirror) flagged to `dxf_flags.md` as a **label-position** check — the connectivity (Cx4 8-2, SG4a 4-8, SG4b 8-2, BS4 2-8) is coherent, so a likely drafting-layout artifact, **not a wrong edge**; Stage C the four `[?]` confirmed **against the drawing** (agrees with freeze §5 + deck: SG3a 1-7, SG4a 4-8, BS3 3-7, BS4 2-8, backstops blocking the reverse of the 7→3/8→2 fire); Stage D the **wire-graph net-traces** the spatially-separable edges exactly to the deck (**C_R→9-10** with its label between ND9/ND10, SG1→2-5, SG2→3-6, C2→4-6), and the **motor is two symmetric banks of 6** (A 11-16 near ND1/2, B 17-22 near ND3/4) matching the manifest. **No edge disagrees.** **Method note (honest):** the dense doubler/commutation symbols are raw geometry (lines/circles/splines, not blocks-with-terminals), so an *isolated per-symbol* terminal trace isn't robustly extractable — those edges are confirmed by node-layout + symmetry + freeze §5 + the traced subset (not contradicted) — a tooling caveat, not a discrepancy. **Verdict `TOPOLOGY-CONFIRMED`** (+ one cosmetic ND7/8 DXF flag): the full 42-component graph is drawn and consistent with deck v0.3 everywhere checkable → **the graph LOCKS** for the shuttle/Cem model upgrade (retire the W_COLL constant + the Cem P_CORE/P_MOTOR stubs) and **v0.11** (which freezes sim+DXF+`.cir`+doc with r0.15 as the geometric authority of record and the DXF-sourced edge list as the manifest). Deck v0.3 stands confirmed — no v0.4 correction needed (the only flag is a DXF cosmetic). Frozen/DXF read-only; **not merged**. *(Supersedes the r0.2 precondition-unmet result now that r0.15 is committed.)*
- **Topology recon r0.2 — net-for-net vs the r0_13 EE schematic → `TOPOLOGY-INCOMPLETE` (gating precondition unmet) (branch `topology-recon-r0_13` off `main`)** — r0.2 was gated on the **r0_13 EE schematic being committed** so its drawn nets could be traced + diffed net-for-net against deck v0.3. **r0_13 is NOT in the repo:** an exhaustive scan of *every* `.dxf` blob across *all* git history finds only three files (r0_6, r0.1, r0.2), **all radial-layout templates** (no ND9/10, no motor, no schematic/wire layer, 0 INSERTs) → there are **no drawn nets to trace**, so the net-for-net diff **cannot run**. Rewrote **`sim/topology_recon.py`** r0.2 (exhaustive cross-history DXF inventory + a gate that **auto-detects** a real schematic by ND9/10+motor and proceeds to the net-trace when one appears; **refuses to fabricate a trace of a non-existent file**), `sim/topology-recon-findings.md` r0.2, `dxf_flags.md` (the drawing punch-list), `topology_edge_list.csv` (re-affirmed, **source still freeze §5 + physics, NOT DXF**). DXF + frozen read-only (empty-diff asserted). **Escalation:** this is the **second brief** (r0_15, then r0_13) gated on a schematic DXF that **has never entered the repo** — the recurring blocker is the **schematic itself, not the deck**. The r0.1 result stands and is re-affirmed: the 42-component manifest reconciles from freeze §5 + Block-D + physics, A/B symmetry exact, the four `[?]` resolved (SG3a 1-7, SG4a 4-8, BS3 3-7, BS4 2-8 — backstops block the *reverse* of the 7→3 / 8→2 fire) — but these remain **freeze-sourced, not drawn-net-confirmed**, and the split resonator (ND9/10) + 24 motor branches are **undrawn in any DXF** (representation gap, not a deck error — no edge disagrees). **Verdict `TOPOLOGY-INCOMPLETE`:** the graph cannot be marked DXF-sourced and v0.11 cannot freeze against r0_13 until the schematic is actually committed; `dxf_flags.md` specifies what it must contain (drawn nets, ND9/10, the 24 motor components, the four `[?]` as nets). The deck's connectivity is trustworthy *where checkable* (the 18-component core + the shuttle path), so the shuttle/Cem model upgrade may proceed on that locked sub-graph, but the net-for-net confirmation + the v0.11 freeze wait on committing the EE-schematic DXF. **Action:** commit r0_13, re-run the tracer (it auto-detects + proceeds). Frozen/DXF read-only; **not merged**.
- **S8 — unified coupled model + four-destination energy ledger (does the nest beat the sum of its parts?) → `SYNERGY-GENERIC` (branch `s8-unified-coupled-ledger` off `main`)** — replaces the *stitched* S5–S7 budget (each box solved in isolation, then added) with **one coupled electromechanical model of the whole nest** (split resonator + varicap pump + Cem branches + contra-rotation), instrumented to report energy **by destination per cycle** (storage / circulation / output / dissipation) and answer the question the piecewise method structurally can't: is coupled `E_diss/E_in` below the stitched prediction, and which coupling carries it? **Framing honored — no new energy** (the belt sources every watt; a synergy = lower dissipation *fraction*, not gain). New **`sim/s8_unified_coupled.py`** (custom Python integrator — SPICE ruled out, the shuttle is ngspice-blocked; the fast f₀ ring transient at f₀ resolution with the dielectric as a genuine shunt conductance, the slow Cem/mechanical at PRF, mechanical quasi-static; **consumer of frozen z/reach**, empty-diff asserted), `sim/s8-unified-coupled-findings.md`, `s8_energy_partition.csv`, plots (`s8_partition.png`, `s8_diss_frac_sweep.png`, `s8_conservation_guard.png`). **The hard conservation guard earned its keep:** the first build double-counted the Cem electrical input + mis-booked the C_R chain → the guard fired (`CONSERVATION-VIOLATED`, residual 0.24); rebuilt to one consistent energy chain (belt → pump+collapse → C_R chain → heat; belt → Cems → core/copper/output), after which the **fire-ring ODE closes to 2.3×10⁻⁶** at every tuning point. **§5 checks all pass:** Gate 0 (couplings-off reproduces z **1.2033** / f₀ **637 kHz** / reach **14.95 kV** before any coupling); baseline partition (storage 88.76 mJ, circulation ~1980 mJ diagnostic, **output ~0 W** — motor core-loss-limited per S7, **dissipation 56.1 W**); Stage C each coupling moves `E_diss/E_in` by **+0.0000**; Stage D the fraction & the 56 W floor are **flat across every DOF** (septum garolite↔mica, k 0/0.3/0.6, fire-phase ±15°, cap-scale 0.5/2×) with no sub-stitched minimum (the U-tube/local-wrong probe is negative); Stage E the parametric channel is **dormant** (rotor 300 Hz vs 2·f₀ 1.27 MHz, ~10⁻⁴). **Concrete payoff — the 53 W phantom debunked:** the stitched garolite dielectric (~53 W = 88 mJ/cycle) is **physically impossible** (it exceeds the whole ~28 mJ/cycle C_R chain); the coupled value is **0.033 W** (a 1584× over-estimate) because the **series-DC-hold waveform duty-cycles the AC ring to ~0.03 %** (the coil-topology/S5 topology, NOT a coupling; tanδ is heat, not circulation). So the **septum material is not load-bearing** at the operating point (the "5× tank-loss swing" applied only to the continuous-ring phantom). **Verdict `SYNERGY-GENERIC`:** the nest is the sum of its parts, the floor is real, tuning only redistributes within it — the conservation-tuning question is **closed** and the stitched budget **vindicated**; the only levers are the floor ones (lamination/flux → core, vacuum → windage), exactly where S7 points. Frozen empty-diff asserted; **not merged**.
- **S7 — expanded-circuit operating point (FIRST, gating) + estimated drag budget & spin-up balance → `OPPOINT-HOLDS` + `BALANCE-FAILS` [EST] (branch `s7-expanded-oppoint` off `main`)** — re-establishes the electrical operating point on the *expanded* circuit (varcap.cir v0.2: split resonator L_R1+C_R+L_R2 + the 12 Cems as an explicit load across Ca/Cb), then folds in a drag/spin-up balance. **Hybrid method** (the prior xsim work found the nonlinear flying-bucket shuttle is **ngspice-BLOCKED**, but the galvanic core recovers z and the linear expanded elements are ngspice's strength): **ngspice** for the galvanic regression + the new linear physics (split-resonator f₀, Cem-branch impedance), the **frozen `resonator_sim`** for the unchanged reach, energy-budget + coil-topology for the node transients & pump→motor power, and coarse `ρω³R⁵`-class models for Phase-2 drag (every figure tagged ESTIMATE). New **`sim/s7_expanded_oppoint.py`** + `sim/s7-oppoint-drag-findings.md`, `s7_oppoint_drag.csv`, plots (`s7_phase1_oppoint.png`, `s7_drag_balance.png`); **frozen empty-diff asserted** (doubler_core/shuttle_core/resonator_sim/reference). **PHASE 1 (gating) → `OPPOINT-HOLDS`:** §3 checks all pass — 1A the runnable galvanic deck recovers **z = 1.2042** vs the frozen 1.2033 (Δ < 0.03) *before* enabling new elements; 1B the split (two L_R/2, k=0.3 aiding, sized) preserves **L_total 79.0 µH / f₀ 632 kHz** and **halves the node-to-ground fire transient 35 → 17.5 kV** (no node > 21 kV — the open >21 kV split flag resolved); 1C the Cems are resonant **at PRF** (f_res 300 Hz, |Z| 40 Ω torque-carrier) and a **high-Z spectator at f₀** (2.56 MΩ = 8106 × Z₀) → the **Block-D premise is confirmed** and the f₀ reach ring is **not detuned** (frozen resonator_sim holds 14.95 kV, crowbar idle). **Pump→motor power = 4–14 W [IR/EST]** extracted — the Cems are **pump-limited** (their N·I=1650 A-t capacity would need ~290 W; the doubler nets only 6.15 mJ/fire), **replacing the stale ~14 W S5 surplus** with a load-aware range. **PHASE 2 (estimate) → `BALANCE-FAILS`:** the steady drag is **15.6–21.1 W** (windage 0.06–5.6 W over 1–100 Pa, bearing 0.5 W, **core loss 15 W dominant**); the belt covers the rotor, the Cems must cover the **stator drag ≈ 18 W** vs the 4–14 W motor power → **margin −4.3 W**, and the contra-rotation spin-up **does not close** — the motor is **pump-AND-core-loss-limited** (15 W core loss alone exceeds the available power). A modest core-loss cut (15 → ~8 W) + the upper-route 14 W would flip it to `BALANCE-TIGHT`/`CLOSES`. **The electrical operating point is sound (Phase 1); the open question is the iron/mechanical loss budget — lever: cut the Cem core loss (lamination/less steel/lower flux).** Frozen empty-diff asserted; **not merged**.
- **S6 — reluctance regeneration coupling (L(θ) overlay → rotation sense + best regen node) → `COUNTER-ROTATING` + `REGEN-CLOSES-AT-CORE` + `REGEN-TRIVIAL` (branch `s6-regen-coupling` off `main`)** — decides where the S5 resonator surplus taps into the Block-D C-EMs so it adds counter-rotation torque instead of braking; reluctance torque T=½·i²·dL/dθ is polarity-blind, so only timing decides drive vs brake. **Parallel-producer** (like Block D): reads the Block-D C-EM map + the **frozen** `shuttle_event_angles.csv` fire clock + the S5 surplus; never feeds `solveDoubler4`/`shuttle_core`. New **`sim/s6_regen_coupling.py`** (read-only L(θ) profile + fire-clock overlay + node scoring + A0–A4 + §8 checks; **frozen empty-diff asserted** on shuttle_core/reference/index.html/resonator_sim), `sim/s6-regen-coupling-findings.md`, `s6_lqtheta_overlay.png`, `s6_node_tap_scores.csv`. **A0 self-tests PASS** (L(θ) 60°-periodic, max at alignment/min at mid-gap, group-A/B rising windows offset = half pitch — the Block-D line-51 no-dead-spot invariant). **All 6 §8 checks PASS.** **`COUNTER-ROTATING`:** the fire clock fires group A @ SG3b 16.05° and group B @ SG4b 46.05° — **30° apart, matching the 30° group offset**; both strokes sit **mid rising-L** (15° margin to each window edge), single consistent sign → Block D's counter-rotation assertion **holds against the geometry (verified, not asserted)**, with a ±15° as-built datum tolerance. **`REGEN-CLOSES-AT-CORE`:** node scoring (priority phase > isolation > V-match > non-disturbance) picks the **core-direct 9/10 gap that doubles as the governor** (score 7 vs transfer-cap 6, governor-reroute 5) — one element fires on (core > 15 kV) AND (rotor in rising-L), shedding the excess *through* the Cems; regen stations 16.05°/46.05° (the rising-L window centres) returned as the DXF instruction. **But `+ REGEN-TRIVIAL`:** the surplus (14 W ÷ 600 Hz = **23.3 mJ/fire**) is **0.13 %** of the Cem per-stroke copper loss (E_mag = ½·(μ₀·A_gap/l_gap)·(N·I)² = 14.1 J/coil → 17.7 J/stroke at Q≈30) — a negligible trim, not a sustaining regen; the positive-feedback loop has gain ≪ 1 and is bounded by the 15 kV governor threshold (no runaway). **The architecture works (correctly phased, best node found, stable) but the recycle doesn't pay** — the no-consumer machine stays a build-then-hold dissipator; keep the simpler S5 dissipative governor unless the surplus ever becomes material. Provenance flag: the DXF r0_6 has no C-EM/pole layers (stations from Block D §2; as-built pole datum needs the next DXF rev). Frozen empty-diff asserted; **not merged**.
- **S5 — self-consistent operating point + dissipation → `OPERATING-POINT-STABLE` + `DIELECTRIC-Q-FLAG` (branch `s5-operating-point` off `main`)** — retires the last *assumed* drive-side quantity (the absolute island fire voltage) by closing the pump↔tank↔clamp loop self-consistently, then adds the dissipation budget so the operating point is real, not anchored. **Consumer-only:** the pump scale + island ledger from the frozen `shuttle_core` (regression anchor z=1.2033); the tank + two-tier clamp the **unmodified** `sim/resonator_sim.py`; S5 wraps a fixed-point solver around them. New **`sim/s5_operating_point.py`** (A0–A4 + §8 named checks; **frozen empty-diff asserted** on `shuttle_core`/`reference`/`index.html`/`resonator_sim`), `sim/s5-operating-point-findings.md`, `s5_operating_point.csv`, plots (`s5_operating_margin.png`, `s5_dissipation_budget.png`); + a one-line S5 update routed onto the s2recheck-s3 "15.5 kV tight" caveat. **A0 gates PASS** (f₀/Z₀ 635.5 kHz/315 Ω guards 123 µH/960 pF leaks; regression z=1.2033; eta 0.990; frozen clean). **All 7 §8 checks PASS** (incl. energy balance E_inj=lossR+stored+crow+glow rel 2.5e-4, ceiling guard, convergence, margin). **`OPERATING-POINT-STABLE`:** the loop pins V_fire to the **gap** (not the anchored 20 kV) — at the recommended **6.0–6.5 mm** gap V_fire = **18.7–20.3 kV** (< 21 kV C1/C2 ceiling), the governor parks V_tank at 15 kV with the crowbar idle, and the cold single-kick peak gives **+12.2 % / +21.5 % margin** (vs the old 5.5 mm gap's tight +2.8 %) across Q = 320/500/900 — **the S3 "15.5 kV cold is tight" caveat is RESOLVED and PINNED**, η_build = 0.795. **Dissipation budget (the new piece):** governor sink 14.4 W (parked-hold shed), arc 0.05 W, windage/glow ≈ 0 (vacuum), bearing ~O(1 W) [RH]; and a **`DIELECTRIC-Q-FLAG` [IR]** — garolite tanδ≈0.02 → Q_diel≈50 makes the dielectric **96 % of the AC ring loss** (53.6 W of the lumped 55.8 W), which would `DISSIPATION-LIMIT` a *parallel-ring* tank, **but the v0.11 series-DC-hold topology (coil-topology) is robust to it** (the reach is single-kick / Q-**independent**, and the DC hold has no AC dielectric loss) → hold power ≈ **14 W (series-hold)** vs ≈ 70 W (parallel re-ring). **Build dynamics:** M2 single-kick (1 fire → 15 kV, 1.67 ms) cross-checked against the multi-fire reality (~6 fires / 10 ms, the M2-PARTIAL series-DC accumulation from island-charging/series-resonator). Terminal drive-side characterization — no consumer (TMD) → no S6; remaining (TMD-gated) S4 vacuum-gap flashover + thermal anchor on this settled point. Frozen empty-diff asserted; **not merged**.
- **Split L_R + bifilar self-resonant coil study → Part A `SPLIT do-it` (node stress ×0.50, clears C1/C2 overstress) · Part B `ANTINODE-STRESS-DOMINATES` → `SIMPLE-DOWNBACK-PREFERRED` (branch `coil-topology` off `series-resonator` `b62e642`)** — evaluates (A) splitting L_R into two L_R/2 halves at each end of C_R, and (B) the per-hub coil topology the split opens (simple down-and-back vs Tesla-bifilar treated as a **self-resonant helical resonator**). New **`sim/coil_topology.py`** (split + mutual-coupling model + distributed/helical self-resonant treatment + 7 self-tests; geometry from the **DXF r0_6 + freeze-doc §3** — *not* index.html; inherits L_R 79 µH/C_R 789 pF/f₀ 637 kHz, the 36-turn conical coil, the series ~35 kV node-5 transient, and the rev-3 C1/C2 21 kV binding + creepage flag; **0 producer edits**, frozen incl. index.html byte-identical asserted), `coil-topology-findings.md`, `coil_topology.csv`, plots (`coil_transient_split.png`, `coil_standing_wave.png`, `coil_srf_modes.png`). **Ordinary distributed-circuit EE — no DCCREG** (L, M, self-C, SRF, standing-wave profile, helical Q; refs Medhurst, helical-resonator theory, Kuffel/Zaengl). **7 self-tests PASS** (a split M=0→L_R; b 35→17.5 kV node-to-ground; c down-and-back fields-add 300 µH, opposed→15.8 µH non-inductive trap; d self-C bifilar 790 ≫ simple 4.8 pF; e SRF lumped 8.2 MHz vs quarter-wave 6.4 MHz agree; e2 bifilar SRF tuned to f₀; f antinode stress hand-calc). **Part A (clean win, DO IT):** `SPLIT-L` two L_R/2=39.5 µH aiding (k 0.30) → L_total 102.7 µH, f₀ 637→559 kHz (restore via C_R 789→608 pF or size the halves); `TRANSIENT-RELIEF` the ~20 kV L_R swing shares as two ~10 kV drops → node-to-ground **35→17.5 kV (×0.50)**; the asymmetric 35 kV *overstresses* the C1/C2 7 mm-air binding (21 kV) on every fire, and the split brings it to 17.5 kV **below 21 kV** — it fixes a real overstress, and clears a clean/ribbed creepage edge (≥30 kV) → buys margin above the 15 kV op before r0_10 is drafted. **Part B:** `DOWNBACK-L` fields-add 300 µH (opposed = the non-inductive trap, flagged); `SELF-C` simple 4.8 pF (~solenoid) vs Tesla-bifilar 790 pF (×165, tuned to land SRF on f₀); `SELF-RESONANCE` simple SRF **8.2 MHz ≫ f₀ → lumped inductor**, bifilar SRF **tunable to 637 kHz = f₀** (design point), G≈Q≈**408**, coupled even/odd modes **f₊ 559 / f₋ 761 kHz** (symmetric fire selects f₊), Q_sr **408** comparable to lumped ~500 — the self-resonant coil (AC ring in the coil, DC in C_R) is *electrically* viable. **But the decider — `ANTINODE-STRESS`:** the quarter-wave antinode concentrates the resonant voltage onto a few inter-filar gaps at **2.9 kV/mm (split) to 5.8 kV/mm (unsplit)** — at/above the **3 kV/mm air limit** (margin only 1.03× even at the clamped lower bound; G≈408 magnifies any unclamped buildup, and the tighter filar gap needed for the self-C worsens it), manufacturing the **air/surface failure mode rev 3 identified, inside the winding**. **Top-level `ANTINODE-STRESS-DOMINATES` → `SIMPLE-DOWNBACK-PREFERRED`:** do the split; take the simple down-and-back (low self-C, lumped, operate ≪ SRF); the self-resonant bifilar's upside is real but the antinode air/surface stress kills it for this machine. Dispositions reported (`PARASITIC`/`LUMPED-TANK`/`SELF-RESONANT`) for the TMD gate. Frozen modules byte-identical; not merged.
- **Battery energy capacity (rev 3): re-run from DXF + freeze-doc (index.html is STALE) → `BINDING-ELEMENT`=C1/C2 7 mm air/21 kV, `BATTERY-CEILING`=21 kV/174 mJ, `DIELECTRIC-NOT-LIMIT` (garolite 180 kV), creepage FLAGGED (branch `battery-capacity-dxf` off `series-resonator` `b62e642`)** — **source correction:** rev 2 pulled geometry from `index.html` and called it authoritative; that is **wrong — `index.html` is STALE**. Re-derived from the authoritative geometry (the **r0_6 DXF** via ezdxf + the **consolidated freeze-doc `varcap-design-freeze-v0.10.md` §3**), which **reverses rev 2's two stale "corrections"**. **STANDING CORRECTION (all future work): the geometric reference is the DXF + the v0.10 freeze doc; `index.html` must not be used for dimensions (only as a flagged `[STALE]` cross-reference).** Rewrote `sim/battery_capacity.py` (DXF-sourced survey: reads radii + insulation layers via ezdxf, axial gaps from doc §3, cross-checked on r387; **0 producer edits**, frozen incl. index.html byte-identical asserted), `battery-capacity-findings.md`, `battery_capacity.csv`, plots. **rev 2 errors reversed:** C1/C2 is **7.0 mm air → 21 kV** (not the stale 0.5 mm/1.5 kV) and doc §4 states it *caps the island at ~21 kV* (fire window 16.6–21 kV) → the documented system ceiling, back in contention; C_R is **12 mm garolite → 180 kV** (not the stale 10 mm mica/400 kV); and v0.10's C_R is **annular at r387** — rev 2's "central void 20 mm/creepage 30 mm" were stale index.html artifacts. **6 self-tests PASS** (a C1/C2 7 mm=21 kV; b garolite 12 mm=180 kV; c DXF↔doc r387 cross-check; d Cx air+mica air holds 98 %; e DXF carries the insulation layers; f fire gap 5.5 mm=16.5 kV in-window). **Verdicts:** `BINDING-ELEMENT` = **C1/C2 7 mm air, 21 kV** (documented ceiling, doc §4); `BATTERY-CEILING` = **21 kV / 174 mJ**, operating 15 kV/88.8 mJ → **1.40× V, 1.96× E margin** (matches the doc's comfortable window); `DIELECTRIC-NOT-LIMIT` — garolite bulk 180 kV ≫ 21 kV (**8.6×**), robust to the source error (held for stale mica 400 kV, holds for real garolite 180 kV). **⚠ FLAG (load-bearing, to TMD):** the **C_R edge creepage is undimensioned** in the r0_6 *radial* template — a bare-edge estimate (≈12 mm septum) gives 12–30 kV, at/below the 15 kV operating point, so a validated design must have radial overhang/ribs the radial DXF doesn't show; it is the binding unknown, *excluded* from the ceiling (rather than letting a guess override the documented 21 kV) pending the not-yet-drafted **r0_10 DXF**. **Provenance flags:** the brief's "DXF v0.11" is **absent** (repo has r0_6 + v0.10 doc; doc §8 cites the missing r0_10); the r0_6 DXF is a 2-D radial template (radii only — R387 confirmed; axial gaps from the doc). **RAISE-LEVER:** widen C1/C2 air (+3 kV/mm), C_R creepage ribs/overhang (resolve r0_10 first), guard-ring the plate edges — not the garolite. Frozen modules byte-identical; not merged. *(Supersedes the rev-2 `battery-capacity` branch, which used stale index.html dimensions.)*
- **Series-resonator output stage (L_R in series with C_R, 5–6) → `SERIES-HOLDS-DC` · `REACH-DC-15kV` in 6 fires · `OVERSHOOT-SELF-LIMITS` · `QUENCH-SERIES-CLEAN` · `DC-AC-SPLIT={15kV, 20kV}` · η_machine 0.445 (branch `series-resonator` off `island-charging` `d0ef6f6`)** — re-sims only the post-fire output stage with L_R *in series* with C_R (5 — L_R — junction — C_R — 6) so **C_R blocks DC and becomes the DC battery**, dissolving the multi-fire accumulation problem the parallel tank (L_R ∥ C_R, shorts DC, rings down) could never hold. New **`sim/series_resonator_output.py`** (series fire ODE + accumulation loop, own RK4 integrator; consumes the inherited island source Q_isl 1.395 µC / E_fire ~14 mJ / W_coll 12.45 mJ @ `d0ef6f6`, W_mech,stator 15.94 mJ @ `84fcaaa`, fire-ODE form @ `05ccf60`; **0 producer edits**, `shuttle_core`/`reference`/`index.html` byte-identical asserted), `series-resonator-findings.md`, `series_resonator.csv`, plots (`series_vcr_accumulation.png`, `series_5_6_waveform.png`, `series_dc_ac_split.png`). **Key coupling:** C_R holds a standing bias, so the gap strikes at V_island−V_CR=V_breakdown (~20 kV) — the island collapses *further* (C_fire 70→42 pF) as V_CR climbs 0→15 kV; the island can over-reach (175 kV ceiling) so the **15 kV clamp, not the island, caps V_CR**. **6 self-tests PASS** (a series half-period quench 0.225 µs; b C_R holds DC & accumulates; c converges to steady 15 kV; d clamp self-limit, island over-reaches to 15.1 kV; e per-fire energy closure rel 6e-8; f inverse experiment — force-drain → no accumulation, recovering the parallel-tank ring-down). **Result:** C_R accumulates to **15 kV DC in 6 fires** (~14 mJ/fire) — the inverse of the parallel-tank ring-down. Despite a mismatched mid-collapse swap (island fires at ~70 pF into 789 pF → only ~30 % energy transfer per fire), the small high-V island **charge-reverses** through L_R, dumping a large ΔQ ≈ 2.56 µC/fire, so the 20 kV breakdown differential re-established each fire pumps C_R to the clamp in 6 kicks. The gap quenches at the series current-zero (~0.17–0.23 µs), C_R left charged. The steady 5–6 waveform decomposes into a **flat 15 kV DC bias on C_R + a brief 20 kV AC transient across L_R** per fire. **η_machine = 0.445** (corner-independent; arc 20/35/50 V identical — the swap is breakdown-differential-driven, not arc-limited): the C-C taxes are unchanged from the island-charging block (~0.48), but the energy now **stays** on C_R instead of ringing away, so the multi-fire reach **succeeds and holds**. The earlier escalation (reach is multi-fire, not single-kick) stands, but the series topology makes that multi-fire reach achievable — C_R is the DC battery, no separate hold cap, L_R doing double duty (resonant charge + current-zero quench). Frozen modules byte-identical; not merged.
- **Island-charging steady-state co-sim → resolves M1 vs M2 → `M2-PARTIAL`, `ISLAND-FIRE-ENERGY = 14.0 mJ`, kick-count 6.5, η_machine 0.484 (physical) (branch `island-charging` off `machine-energy-balance` `05ccf60`)** — sources the island fire self-consistently, replacing the prior block's unsourced 130 mJ assumption (which gave the impossible η_machine = 7.3 > 1) and resolving the M1-vs-M2 coupling choice deferred since project start (`s2_coupling.py:14` was BLOCKED on it; `run_X1` anchored the island at 20 kV *by assumption*). New **`sim/island_charging_cosim.py`** (steady-state iteration + charge/energy ledger are new; consumes the frozen cap topology Cx 8↔648 pF + 8 pF boss, Ca/Cb 309 pF, z=1.307, and inherited anchors η_fire 0.985 / W_mech,stator 15.94 mJ / useful_per_fire 6.15 mJ; **0 producer edits**, `shuttle_core`/`reference`/`index.html` byte-identical asserted), `island-charging-findings.md`, `island_charging.csv`, plots (`island_energy_ledger.png`, `island_kickcount.png`, `island_rail_sensitivity.png`). **Decisive physics:** E_fire = ½·Q·V_strike is set by the charge the rail sources, bounded by two independent per-cycle ceilings — the **charge/dilution** ceiling (309 pF rail diluted by the 648 pF island to 6.46 kV on first pickup; the pickup is a C-C equalization, the same two-cap tax as the stator core, keeping only **24 %** of drawn energy) and the **energy/budget** ceiling (doubler nets only **6.15 mJ/cycle**). **5 self-tests PASS** (a M1 literal limit = 1.58 mJ continuity; b steady-state convergence rel-spread 2e-16; c ledger closure rail-draw+collapse=E_fire+pickup-loss rel 8e-16; d M2 ceiling 12.96 µC/129.6 mJ; e rail-dilution 6.46 kV < 20 kV). **Verdict `M2-PARTIAL`:** the physical spark gap fires **mid-collapse** at ~70 pF every cycle, delivering **E_fire ≈ 14.0 mJ** (robust 14–17 mJ over the rail sweep) — an order below the 130 mJ M2 reservoir but well above the 1.6 mJ M1 bucket. So the 89 mJ tank takes **~6–7 fires**: the **single-kick reach is WRONG (multi-fire) — escalate**; the "isolated knock" must be reframed as a pumped swing of ~6–7 × 14 mJ. **η_machine = 0.484 is now physical (<1), resolving the prior >1 paradox:** the literal-1.6 mJ block charged W_mech,island with the wrong (8 pF bucket) charge; at the sourced charge (1.40 µC) the **rotor's collapse mech work ½Q²Δ(1/C) = 12.4 mJ dominates E_fire** (the rail only seeds 1.5 mJ after the 4.7 mJ pickup tax), and once counted on both ledger sides η_machine = **0.48–0.55** (not the optimistic 0.88). M2 fails two ways: the gap fires mid-collapse before 648 pF is reached, and *forcing* M2 needs **37 accumulation cycles/fire** (η_machine 0.22). The reach floor is still clearable in energy but not in one kick; the margin must be re-derived in the multi-fire regime. Frozen modules byte-identical; not merged — re-opens the reach for TMD review.
- **Island→tank fire-path energy balance + whole-machine efficiency → `FIRE-PATH-RECOVERS` · `S2-ETA-CONFIRMED` · `QUENCH-AT-ZERO-CLEAN` · `ENERGY-BALANCE-GAP` (η_machine>1, escalate) (branch `machine-energy-balance` off `energy-balance` `84fcaaa`)** — the machine-level answer to "mechanical work in vs tank energy out". The stator block found the 4-node core only 38.6 % efficient (61 % C-C tax) but flagged the ~89 mJ kick is the **island Cx→tank C_R** fire, an inductive **C-L-C** swap that should bypass that tax. New **`sim/fire_tank_transfer.py`** (a **new time-domain RK4 ODE** — NOT consumer-only; the frozen quasi-static solver shorts the 5–6 tank and cannot do the L-coupled dynamics — but all *state* is consumed from the frozen `shuttle_core` trace + Block R; **0 producer edits**, `shuttle_core`/`reference`/`index.html` byte-identical asserted), `machine-energy-balance-findings.md`, `machine_energy_balance.csv`, plots (`fire_transfer_trace.png`, `fire_loss_ledger.png`, `machine_eta_vs_corner.png`). The loop ODE `L_eff·dI/dt = V_island−V_tank−V_arc·sgn(I)−I·R_loop` (+ dV_isl/dt=−I/Cx, dV_tank/dt=+I/C_R) integrated from the pre-fire island (Cx 648 pF @ 20 kV → 130 mJ) to the **first natural current-zero** (no cutoff). Params cited (C_R 789 pF, L_R 79 µH, V_arc 20/35/50 V + τ_rec 10µs/100µs/1ms from `shuttle_core.py:685-687`); **L_loop≈1 µH / R_loop≈0.5 Ω flagged `[IR]` undocumented**, swept, both sub-dominant (L_eff≈L_R; loss arc-dominated). **5 self-tests PASS** (a lossless matched swap η→1; b arc-dominated η collapses 0.98→0.36, localizer attributes to arc; c energy closure delivered+arc+loop+residual=E_pre to 6.9e-8; d 88.8 mJ tank scale; e τ_quench=0.530 µs=π√(L_eff·C_eff)). **The fire path RECOVERS:** η_fire = **0.985** (opt/mid/pess 0.9868/0.9853/0.9838), reproducing S2's matched-cap **0.9904** within ~0.5 % (arc drop + ~1 % first-zero residual on the mismatched 648/789 island) → **`S2-ETA-CONFIRMED`** + **`FIRE-PATH-RECOVERS`** (98.5 % vs the stator's 38.6 % — the inductive swap bypasses the C-C tax). **`QUENCH-AT-ZERO-CLEAN`:** extinguishes at the first current-zero (0.530 µs); arc τ_rec (10µs–1ms) ≫ ring-back (2·τ_quench = 1.06 µs) → no re-conduction before the tank rings back, at every corner (the S3-deferred local-loop question, answered). **But `ENERGY-BALANCE-GAP` (escalate):** per the user's **literal-trace** decision, `W_mech,island` is the flying-bucket ½Q²Δ(1/C) from the frozen trace = **1.58 mJ** (the collapsed 8 pF bucket holds only 1.6 mJ at 20 kV), so `η_machine = E_tank,delivered/(W_mech,stator 15.94 + W_mech,island 1.58) = 127.7/17.5 = ` **7.3 > 1 — UNPHYSICAL.** The efficient fire delivers ~128 mJ from a 648 pF/130 mJ island, but the **literal flying-bucket shuttle trace performs only 17.5 mJ of mechanical work per fire** → the **M2 island-dump (the 130 mJ reservoir the S2/S3 89 mJ single-kick reach relies on) is not energetically sourced by the M1 flying-bucket trace** the rest of the campaign runs on. The gap names the unaccounted ~130 mJ island-charging work: either it is a real (unmodelled) mechanical input (M2 real → η_machine≈0.88) or the literal machine delivers ~1.6 mJ/fire and the tank needs ~80 fires (not single-kick). **The reach margin rests on an island-charging energy the shuttle trace does not contain** — re-examine via the deferred self-consistent S5 co-sim. The fire *path* is not the problem (it is efficient); the *sourcing* of the M2 island is. `MACHINE-ETA` is a flag, not a number. Frozen modules byte-identical; not merged — left for TMD review.
- **Electromechanical energy balance (mechanical shaft work in vs tank energy) → `ENERGY-BALANCE-CLOSES` · `EQUALIZATION-TAX-PRESENT` (branch `energy-balance` off `s2recheck-s3-spark`, the 789 pF head)** — a **second conservation law** (energy) laid over the charge-law z campaign; de Queiroz's z explicitly excludes electromechanical forces, so this fills that gap and cross-checks the pump from the energy side. New **consumer-only** `energy_balance_from_solver.py` (+ 5 self-tests), `energy-balance-findings.md`, `energy_balance.csv`, `energy_balance_flow.png` (W_mech → ΔE_stored + E_tax bar), `energy_balance_tau.png` (τ profile). Reads the FROZEN `solve_doubler4(trace=True)` ("traceDoubler4") + reuses `charges_from_voltages`; recomputes **no** electrical solve — the only linalg is the *constant-charge* cap-change step (pure mechanics, no diodes) + energy bookkeeping. **0 producer edits** (`shuttle_core`/`reference/doubler_core.py`/`index.html` byte-identical to base, asserted). **Base flagged:** the 789 pF *design* is on `freeze-v0.10` (varcap doc) but the runnable *operating-point preset* `G3-geometry-v010.json` (cR 789) + tank infra are on `s2recheck-s3-spark` → built there. **Method:** inject the G3 caps into the 4-node galvanic core (pumps z=**1.334** = the known ceiling, cross-check); per phase transition split ΔE_stored into the **mechanical stroke** `W_mech = U(Q,C_new)−U(Q,C_old)` (constant-Q, diodes off) and the **diode equalization** `E_tax = U(pre-diode)−U(post-diode)≥0`; the pre-diode endpoint is the constant-Q solve `M(C_new)·V_int=Q` (cap matrix = ∂Q/∂V of the frozen charges fn). **Self-tests PASS** (a single-cap ½Q²Δ(1/C); b constant-Q round-trip 1e-13; c a deliberate mismatched merge recovers a *known* ½(C₁C₂/(C₁+C₂))ΔV² tax — positive control for the localizer; d τ-mean=W/Δθ; e ½C_R·V²=88.8 mJ ≈ S3 floor). **`ENERGY-BALANCE-CLOSES`:** the per-cycle identity `W_mech = ΔE_stored + E_tax` holds to **1.4×10⁻¹⁶** (machine precision), and the **eigen-growth cross-check** `ΔE_stored/U_start = 0.7796 = z²−1` to 2×10⁻¹⁶ — the energy bookkeeping reproduces the charge-law pump growth exactly ⇒ the z campaign is **energetically self-consistent** (independent, second-law confirmation). **`EQUALIZATION-TAX-PRESENT` (the named sub-outcome):** the ideal charge-conservation transfer carries a real, quantified capacitive-equalization loss — **61.4 % of W_mech** at the G3 swing (the two-capacitor paradox at every diode merge, ½CΔV² even with ideal diodes) ⇒ stator-core conversion efficiency **η_conv = 38.6 %** — a real efficiency tax the charge-only analysis could not see. **`ENERGY-BALANCE-GAP` NOT triggered** (no residual beyond the quantified tax). **Honest scope `[IR]`:** absolute per-fire W_mech (15.9 mJ at the 15 kV rail anchor) is ≪ the 89 mJ tank kick because the kick is **island Cx-boost dominated** (not in this stator core); so η is the core conversion `ΔE_stored/W_mech`, **not** the invalid cross-layer `E_tank/W_mech` (>1 — surfaced as a deliberate tell, not smoothed). **η and τ are reported** (η_conv 0.386; τ-ripple ≈7.8, the Block-D motor load) — and **separable** (energy uses only endpoints → robust to C(θ) shape; only τ uses dC/dθ). Frozen modules byte-identical; not merged.
- **S2 reach re-confirm @789 pF (B1) + S3 spark tier (B2–B4) → `REACH-CONFIRMED-789` · `STRIKE-CONFIRMED` · `QUENCH-OK` · `BACKSTOP-CLEAN` · integrated reach clears 15 kV (branch `s2recheck-s3-spark` off `freeze-v0.10`)** — the one combined pass validating the v0.10 freeze (C_R 960→789 pF). New `presets/G3-geometry-v010.json` (G2 clone, `cR` 960→789, +12 mm-disc provenance; G1/G2 not mutated), `sim/s2recheck_s3_spark.py` (**consumer-only**, regression-gated; reuses frozen `shuttle_core` spark machinery + drives the standalone `resonator_sim` tank at 789 pF), `sim/s2recheck-s3-findings.md`, `sim/s3_spark_traces.png`, `sim/s3_spark.csv`. **Gates PASS:** ring f₀/Z₀ **637.5 kHz / 316 Ω** (guards a stale 123 µH/960 pF leak); G3 loaded==expect; **C0 regression** (`assert_ideal_identity` + T0a/T0b/T0c → anchor 1.2033, ideal 1.1894, ledger drift 4.6e-14); frozen empty-diff (checked twice). **B1 entry gate `REACH-CONFIRMED-789`:** smaller C_R is favourable — η 0.962→**0.990**, floor 108→**89 mJ**; island@20 kV → E_M2 **128 mJ** → cold-tank 18 kV → holds 15 kV (easier than the 960 pF run). **B2 `STRIKE-CONFIRMED`:** the drafted 5.5 mm SG3b/SG4b gap (12 mm spheres, 3 kV/mm) strikes the island at **17.2 kV**, inside the 16.6–21 kV window (near the low edge; `paschen_strike`'s 0.5 mm regime deliberately not used). **B3 `QUENCH-OK` + `BACKSTOP-CLEAN`:** arc τ_rec (10 µs–1 ms) ≪ the 1.67 ms favourable half at all corners; frozen `C2_backstop` BACKSTOP-CLEAN (island 0.71× single-bucket ≤ 1.05, a **scale-free** ratio → holds at the v0.10 strike); **BS3/BS4 DXF markers still TODO** (sim runs parametrically on the freeze §5 spec). **B4 integrated reach:** at the **real** fire-gap strike (island 17.2 kV, not the 20 kV anchor) → **94 mJ > 89 mJ floor** → cold-tank 15.5 kV → two-tier clamp holds 15 kV, crowbar idle, sink 3.6 W, conservation +0.03 %. **Honest caveat:** 15.5 kV cold is tight → recommend opening the gap to ~6.0–6.5 mm (island 18–20 kV) for margin, below the 21 kV C1/C2 flashover. **Closes the S2 fire-gap conditionality at 789 pF.** `shuttle_core`/`reference`/`index.html` byte-identical; not merged.
- **S2 pump↔tank coupling (does the real pump deliver the assumed kick?) → `PUMP-DELIVERS-EASED-ONLY` (M2) / `PUMP-UNDERDELIVERS` (M1) — map-dependent (branch `s2-pump-tank-coupling` off `resonator-sim-r2`)** — replaces the parameterised `E_kick` in `resonator_sim` with the **real per-cycle energy the frozen pump delivers** at the grown (r2) geometry, and asks whether it clears the 108 mJ reach floor. New `presets/G2-geometry-r2.json` (G1 clone with island Cx 4/88→**8/648 pF**, C_R 1477→**960 pF** record-only, L_R 79 µH provenance; C1/C2 + Ca/Cb unchanged; G1 not mutated), `sim/s2_coupling.py` (**read-only consumer** of the frozen pump driving the **unmodified** `sim/resonator_sim.py` tank), `sim/s2-coupling-findings.md`, `sim/s2_coupled_traces.png`, `sim/s2_E_deliver.csv`; brief saved to `docs/briefs/`. **All §8 checks PASS:** ring f₀/Z₀ 577.9 kHz/287 Ω (guards a stale 123/169 µH leak), G2 loaded==expect, G0 anchor (inj 1.20327), ledger drift 1.2e-13, frozen empty-diff, coupled-chain energy conservation < 0.05 %. **G0** injection VERIFIED; G2 galvanic ceiling 1.334 = G1 (`galvanic_z` independent of Cx/C_R). **P1 — grown bucket pumps HARDER:** z_shuttle **1.2077 (88 pF) → 1.3072 (648 pF)** (+48 % above unity, below the 1.334 ceiling), ledger clean (1.2e-13, no `LEDGER-BREAK` — the 648 pF island is a clean shuttle), boost_ratio 40.5. **TMD chose to run BOTH coupling maps (brief §4).** **X1 scale-fix:** the ideal pump is **scale-invariant** (eigenvector grows by z, no absolute volts), so the absolute delivery is set by the operating-point anchor (island fire = design HV 20 kV, `[IR]`), not the pump. **The two maps BRACKET the 108 mJ floor:** **M2 island-dump** (η = 96 % LC transfer of the 648/960 pair) → **124.7 mJ → `PUMP-DELIVERS-EASED-ONLY`** (cold-tank 16.1 kV); **M1 rail-increment** ((1−1/z)·C_R·V² = 90.2 mJ) → **`PUMP-UNDERDELIVERS`** (13.7 kV). **C1:** driving the unmodified tank, M2 delivery **HOLDS 15 kV** (governor sheds 10.5 W, crowbar idle, all Q); M1 under-reaches 13.7 kV. **Working verdict `PUMP-DELIVERS-EASED-ONLY`** (M2, the migration-doc island-dump): eased reach is real, the ~6 % margin is the true ceiling, full-drive +25 % headroom was **paper**; M1 is the conservative bound. Result is **map- and scale-sensitive** — the absolute island fire voltage (anchored 20 kV; 18/20/23 kV → 101/125/165 mJ) is the swing variable, to be pinned by the self-consistent S5 co-sim. **Resolves r2 conditionality:** `TANK-HOLDS-15kV` stands under M2/eased, conditional-unmet under M1. `shuttle_core`/`reference`/`index.html` byte-identical (asserted); not merged.
- **resonator-sim r0.2 (revised geometry; confirm 15 kV + clamp hold) → `TANK-HOLDS-15kV` (branch `resonator-sim-r2` off `resonator-sim`)** — re-run of the standalone tank+clamp model at the **revised geometry** that closes the r0.1 17× reach gap. The three coordinated changes (brief §2–3): island **Cx 88→648 pF** (now ≈ tank C_R → efficient charge-sharing), **C_R 1477→960 pF** (8 mm garolite), and the **conical-hub L correction 169→79 µH** (the cylindrical 169 µH was a mis-model; f₀ 318→**579 kHz**), with the target softened **20→15 kV**. Code moved to `sim/resonator_sim.py` (re-parameterised; new `sim/resonator-r2-findings.md`, `sim/resonator_r2_traces.png`, `sim/resonator_r2_sink.csv`; brief + hydraulic-philosophy doc saved under `docs/briefs/`). Still **standalone** (imports none of `shuttle_core`/`reference/doubler_core.py`/`index.html`); numpy+matplotlib hand-rolled RK4. **R0 PASSES** (f₀ 577.9 kHz vs analytic 0.00 %, τ 0.01 %) — **re-validates the corrected conical 79 µH** (the 169 µH cylindrical model would give 318 kHz, falsified); energy-conservation gate < 0.03 % across all stages. **R1 reach CONFIRMED:** full drive (171 mJ) → **18.8 kV**, eased (115 mJ) → **15.4 kV**, both ≥ 15 kV at all Q; **single-kick** (accumulation ×0.99 — still kick-and-decay at 600 Hz) so reach is **Q-independent**; **+25 % headroom**, and the full drive can fall 37 % (171→108 mJ) and still hit 15 kV. **R2:** confirms the r0.1 placement finding — a **void** shunt bleeds but the impulsive kick spikes the peak past V_glow (peak ~18 kV); the **upstream/island** governor caps the peak **exactly at V_glow** (15 kV). **R3:** crowbar **idle** under eased nominal drive at ≥ 16 kV setpoint; catches only the full-drive runaway, dumping ~168 mJ/event (~17 MW/10 ns). **R4 the headline:** the two-tier upstream governor + crowbar **HOLDS the tank ≤ 15 kV with the crowbar idle at all Q, both drive modes**; governor **sink load ~39 W (full) / ~4.4 W (eased)** — matching the brief's ~38 W / few-W prediction. **Verdict `TANK-HOLDS-15kV`**: the revised resonator + clamp is validated; recommend **eased drive** (~4 W sink) over full (~39 W) unless the void/structure can carry 39 W (a deferred structural-sizing call, not auto-`SINK-OVERSIZED`). The closed gap is the geometry — matching the island vessel to the tank (hydraulic equal-vessel transfer). Next: **S2** pump↔tank coupling (extract real per-cycle 5–6 charge from `shuttle_core`; drive parameterised here). Frozen solvers + `index.html` untouched; not merged.
- **resonator-sim r0.1 (first live 5–6 tank + clamp architecture) → `TANK-UNDERDRIVEN` (branch `resonator-sim` off `geom-shuttle-gate`)** — the **first** time-domain model of the live resonator tank and the full clamp tiers (design-intent-lock §4–7); every prior run **shorted** the 5–6 tank, so the frozen quasi-static `shuttle_core` cannot simulate it. New **standalone** `resonator_sim.py` (driven parallel-RLC + nonlinear clamp shunts; hand-rolled numpy RK4, scipy-free, crowbar as a discrete event-dump; imports **none** of `shuttle_core`/`reference/doubler_core.py`/`index.html`), `resonator-findings.md`, `resonator_traces.png`, `resonator_sink_energy.csv`. Tank from the brief's geometry+materials (L=169 µH, C=1477 pF, **f₀=318.6 kHz**, Q swept 320/500/900), superseding the code's L_RES=123 µH. Drive is the **open lever**: `shuttle_core` exposes no tank-side delivery, so per-kick energy E_kick is **swept** {5,20,50,100,300} mJ, anchored by the 88 pF island estimate ½·Cx·(20 kV)² ≈ **17.6 mJ**. **R0 authoriser PASSES** (free-ringdown f₀ error 0.00 %, τ error 0.01 % vs analytic; energy-conservation gate < 0.1 % across all stages). **R1 headline:** at the physical ~18 mJ/kick the tank reaches only **~5 kV**; 20 kV on the 1477 pF tank needs **~295 mJ (~17×)**, i.e. the 88 pF island at **~82 kV** — implausible. With τ≈0.5 ms vs 1.67 ms kick-spacing (600 Hz, both branches) the tank **decays between kicks** (accumulation ×1.00–1.01) — **kick-and-decay, not resonant**; Q-sweep confirms (spacing/τ = 5.2/3.3/1.9, never < 1, so higher Q cannot close the gap). **R2:** the soft glow governor's **placement is decisive** — a **void** shunt bleeds energy (Q-spoil up to 2.2) but the impulsive ~10 ns spark spikes the peak past V_glow before a µs-scale shunt responds; the **island/upstream** placement caps the peak exactly at V_glow (favours upstream, lock §6.3). **R3:** crowbar **idle** under nominal drive (0 fires); under over-drive it dumps **~491 mJ/event (~49 MW/10 ns)**, threshold-independent (impulse overshoots every setpoint) — the MW-class full-tank dump the lock §6.2 anticipates, sizing the deferred sink. **R4:** the two-tier clamp **HOLDS ≤ 20 kV with the crowbar idle at all Q** at an adequate design drive — so the **clamp architecture is validated**; the open problem is the *drive*, not the clamp. Levers named (grow Cx→C_R, raise PRF so spacing<τ, resonant-sync). Negative headline is the deliverable; nothing retuned. Frozen solvers + `index.html` untouched; not merged.
- **geom-shuttle Phase-7 (first geometry-fed pump) → `GEOM-PUMP-CONFIRMED` (branch `geom-shuttle-gate` off `main`'s canonical solver line `effcee1`)** — the first run of the **real buildable geometry** (DXF r0.6, Z-stack true-scale caps) through the canonical `shuttle_core`, replacing the campaign's placeholder caps. New `presets/G1-geometry-r06.json` (raw geometry caps C1/C2 16/280, Cx 4/88, Ca/Cb 309, C_R 1477 record-only, Cpar 20; `expect` self-test block), `geom_shuttle_run.py` (pure consumer: G0→G3), `geom-shuttle-findings.md`, `geom_caps_vs_placeholder.csv`. **`shuttle_core.py` carries ONE additive, TMD-authorised change** — a default-preserving cap-injection hook `set_device_caps()`/`reset_device_caps()` + `device_caps_selftest()` (+40 lines, 0 deletions; the documented device-point scalars are the injection point, only `Cx` was a `Params` knob). Rev-0.3 path **byte-identical** (hook inert by default): `--rev03` anchor 1.2033, spark C0 `T0a 1.2033 · T0b 1.1894 · drift 4.6e-14` all green; `reference/doubler_core.py` + `index.html` untouched. **G0 INJECTION-VERIFIED, anchor reinterpreted:** placeholder resets to z=1.20327 exactly (injection proven); the **geometry caps' own galvanic ceiling is 1.334** — which **independently reproduces the chat-side port's z≈1.335** (the brief said that number "failed"; the canonical solver confirms it). `Z_BASELINE=1.2033` is the *placeholder* device point's ceiling, not a cap-invariant; the brief-literal "Ca/Cb→large→1.2033" does NOT hold (Ca/Cb≫C1 shorts the pump ⇒ z→1.000), reported as a finding. **G1 the headline:** ideal-tier `z_geom=1.20766 > 1` and **above** the placeholder `z=1.18938` (+9.7% rel.) — the buildable geometry **pumps, harder than the placeholder**, because Ca/Cb=309 pF now exceed the swinging C1max=280 (placeholder reservoir 100 was tiny vs 1000 swing); z_geom sits below its own 1.334 ceiling (consistent). **G2 fires with headroom:** effective boost ratio drops 17.65×→7.33× (fixed boss/gap strays 8 pF dominate cx_min=4), but the scale-free fire margin (island overvoltage / source rail) is **1.16 vs placeholder 0.15** — the larger Ca/Cb load the island higher, so it fires *more* comfortably; the ~14× smaller per-fire charge (Cx 88 vs 1200) is throughput (z>1 survives it), not a fire blocker → **no GEOM-FIRE-MARGINAL** (absolute-strike confirmation deferred to the spark tier once gap electrodes are placed, §7). **G3 firing order HOLDS:** SG1/SG2-lead SG3/SG4-lag at the as-drawn 30°/no-offset (the brief flagged this as "expected to possibly fail"; it does not — no island timing offset required); gap stations (SG1 0.05 … SG4b 0.7675 sector) output as the placement target for the next DXF. Caps consumed from the preset, not hard-coded; consumer re-implements no solver internals. Not merged.
- **xsim Phase-6 rev 0.5 (V0 hardening + X2/X3 + dt-sweep) → `V0-SECONDARY-OPEN` · `X2-B XSIM-MATCH` (3 corners) · `X3-B STRUCTURE-CONFIRMED` · `T3 DT-PLATEAU` (branch `claude/feasibility-approval-planning-5gkcam`)** — extended the (B) analytic eigen-witness to the arc and bootstrap tiers + added a ngspice timestep diagnostic. `xsim_queiroz_matrix.py` gains `_newton_zab` (Newton engine, unit-checked on √2), `queiroz_fig1_newton` (T1), `arc_limit_cycle`/`_arc_phase` (X2), `bootstrap_structure`/`boot_classify` (X3); `xsim_from_solver.py` gains the X2/X3 table rows + `dt_sweep` (T3) + plots (`xsim_x2_arc_corners.png`, `xsim_x3_bootstrap_structure.png`, `xsim_dt_sweep.png`). **T1 V0-SECONDARY-OPEN:** the Newton engine works, but Queiroz's Fig-1 z=1.1538 is NOT reproduced — his segment *restrictions* are given but not his Fig-1 cap/diode **topology**, and the paper is unfetchable in-environment (arxiv/IEEE/coe.ufrj.br all 403); reconstruction attempts give pumping doublers at z∈{1.0,~2.08} not 1.1538, reported as an honest residual (not a 'topology' hand-wave). X1-B authorisation therefore stands on the in-repo galvanic anchor (eigen z=1.2033, Δ=−4e-16, exact). **X2-B XSIM-MATCH (all 3 corners):** the absolute-volt Paschen strike breaks scale-invariance ⇒ z is a limit-cycle property (the genuine §3.1 case); the fire opens at the **absolute strike crossing** (emergent, read not clocked) with partial transfer φ=1−pVarc/ov, and z_arc (geometric-mean growth-window gain of the **independent** solver) reproduces native **opt 1.18877 / mid 1.18440 / pess 1.16666 to MACHINE PRECISION** (Δ≤6e-6), with the island-overvoltage **clamp≈1.01–1.04× strike** (native ~1.04). Strike (650/333/150 V) and pVarc (20/35/50 V) **consumed**, not tuned. **X3-B STRUCTURE-CONFIRMED:** with V_FLOOR=327 (rail no-fire floor) + tau_storage leakage consumed, trajectory classification (no-fire/fire-and-decay/growth) backs out **V_floor < V_sustain**, **V_sustain RISES as rpm falls** (mid 160→230→520 @3000/1519/769), and **pess non-self-sustain ≤3000 rpm** — all reproduced (the structural test, brief §4); magnitudes softer than native (V_floor 120 vs 187, V_sustain 160 vs 437 — the low-V retention model sustains more easily), reported honestly (≤15% magnitude band not met; ordering/direction is the load-bearing test). **T3 DT-PLATEAU:** sweeping the X1-A max-timestep 30× (Δt=T/{400..12000}) leaves z at ~1.05 (1.0544→1.0592, Δz=+0.005), NOT trending toward native 1.18938 ⇒ the X1-A under-pump is **STRUCTURAL** (zero-time cluster re-solve vs finite-rate integrator), not numerical — the §3a mechanism-(1) attribution is now EVIDENCED. **Three independent constructions agree on X1 (native forward · B eigen) and X2 z_arc (native spark · B eigen) to machine precision.** Combined: X1 PASS on B · X2 PASS on B (3 corners) · X3 structure on B; A's divergence is the named, T3-confirmed continuous-time artifact. Independence preserved (grep-gate: 0 native-solver calls). Native modules untouched (mirror FAITHFUL, C0 green); not merged.
- **xsim Phase-6 rev 0.4 (dual witness) → `X1-B XSIM-MATCH` (analytic eigen-witness) · `X1-A XSIM-DIVERGENT` (ngspice) · `X1 PASS on B` (branch `claude/feasibility-approval-planning-5gkcam`)** — added a **second, analytic witness** (B) per the rev 0.4 addendum: `xsim_queiroz_matrix.py`, a **segment-matrix / eigenvalue** solver in the spirit of de Queiroz (*Analysis of Electronic Electrostatic Generators*, IEEE 2018) — z is the **dominant eigenvalue of the per-cycle charge-transfer map** (cycle segmented by conduction state; each segment a nodal charge-conservation relation; composed into one map; eigenvector = steady voltage ratios), with the emergent fire as the collapse sub-step where the boosted island crosses threshold. **Independence verified (addendum §3.3):** B implements its **own** cluster solver and does **not** import `shuttle_core.transition`/`_galv_phase` (grep: 0 calls; consumes only device-point constants + firing **order** + `shuttle_run` as the comparison target) — `shuttle_core` marches **forward in θ** and reads z as the iterated asymptotic ratio, whereas B **never time-steps**, composing segment matrices and extracting z as an **eigenvalue** (genuinely different construction). **V0 authorisation:** the eigen-method recovers the **galvanic anchor z = 1.2033** (Δ=−4e-16, paper-independent, the (B) analog of X0); Queiroz's Fig-1 example (60/360, Ca=Cb=330, paper z≈1.1538) gives 1.326 — does **not** reproduce his number because his exact topology differs from our 4-node galvanic doubler and is **un-fetchable** in-environment (arxiv/IEEE/his site all 403); reported honestly, authorisation rests on the galvanic anchor. **X1-B `XSIM-MATCH-B`:** eigen z = **1.18938** (Δ=−5e-14 vs native) and emergent δ = **0.2175** (Δ=0) — to **machine precision**; δ is genuinely emergent (sweeping the threshold moves it 0.2175→0.36 while **z stays invariant at 1.18938**, reproducing the native pVbkFire behaviour exactly). **Three-way z:** `shuttle_core` (forward) 1.18938 · (B) eigen 1.18938 · (A) ngspice 1.05444 — two independent constructions agree on z **and** δ to machine precision. **The rev-0.3 ngspice (A) is demoted to tertiary** (`xsim_from_solver.py` now runs A, drives B, prints the dual-witness §5 table + three-way line + V0 plot `xsim_queiroz_V0.png`): A still **`XSIM-DIVERGENT-A`** (z=1.0544, δ=0.1917), now **conclusively localised as a continuous-time SPICE artifact** since B matches native — *not* a circuit defect, exactly the addendum's prediction. **Combined X1: PASS on B (primary).** X2/X3 now **admitted** (X1-B passes) but deferred (the eigen arc/bootstrap extension = the genuine Newton/absolute-volt case) — marked BLOCKED-pending, not claimed. Native modules untouched (mirror FAITHFUL, shuttle C0 green); not merged.
- **xsim Phase-6 rev 0.3 (Queiroz unblock) → `X0-RECOVERED` · `X1-UNBLOCKED & PUMPING` · `FIRE-EMERGENT` · `XSIM-DIVERGENT on z` (branch `claude/feasibility-approval-planning-5gkcam`)** — adopted the **De Queiroz source-imposed charge-transfer** method (addendum rev 0.3) to clear the rev-0.1 X1 blocker, **without touching** `shuttle_core.py`/`reference/doubler_core.py`/`index.html`. `xsim_netgen.py` gains `netlist_x1_shuttle` (+ `_island`/`_cx_wave` helpers); `xsim_from_solver.py` gains the X1′ run, the fire-angle readout, the K-invariance sweep and a VOID-METHOD guard; new `xsim_x1_shuttle.net`, `xsim_x1_fire_readout.png`, `xsim_x0prime_anchor.png`; `xsim-findings.md`→rev 0.2. **Islands Cx3/Cx4 are charge-controlled** `V=Q/Cx(t)` (charge on a 1F integrator, terminal via `B`-source; Cx+boss lumped so each island node carries a single cap + gaps): verified in isolation to boost **×20=Cx_max/Cx_min** with charge conserved to machine precision — **the boost is imposed by the source law (ratchet by construction), no stiff behavioral-Q node**. C1v/C2v stay `Q=` caps (proven at X0); return/load gaps are **clocked bidirectional** switches (the ideal-tier native gaps are cluster-merge equalisations, not diodes); **fire SG3b/SG4b is a source-imposed soft-threshold dump** `I=win·gm·max(0,V(isl,snk)−Vstrike)` — one-way, threshold-driven, eligibility=the Cx collapse window, **not a fire clock** (addendum firewall). **The shuttle now RUNS (≈3 s, no timestep collapse) and PUMPS (z>1)** — both rev-0.1 blocker modes cleared. **Fire is EMERGENT (not `XSIM-VOID-METHOD`):** sweeping the strike threshold moves δ **0.192→0.329** (native trend 0.218→0.39) — δ is measured off the boosted rail, not clocked. **K-invariance PASS:** z, δ stable across K=1e8/1e9/1e10. **But X1 z = 1.0544 vs native 1.18938 (Δ=−0.135) is OUTSIDE the ≤0.005 tolerance** (and δ 0.1917 vs 0.2175, Δ=−0.026): the shortfall is **robust across every fire realisation and K** — not a fire artifact — so it is named/localised as the continuous-time shuttle **under-transferring charge per cycle** vs the quasi-static cluster-solve (the island boosts correctly, 0.03→0.32, but the rail node under-boosts between phases). **Anchor-chain:** X0 holds, so the native quasi-static z is authoritative and the SPICE under-pump is the localised artifact → **`XSIM-DIVERGENT` on z** (a deliverable, not a failure: the shuttle limit needs a per-event imposed equalisation closer to the cluster-solve to close the ~0.135 gap). X2/X3 stay BLOCKED by strict campaign order (X1 not within §5). Native modules untouched (mirror FAITHFUL, shuttle C0 green); not merged.
- **xsim Phase-6 SPICE cross-validation → `X0-ANCHOR-RECOVERED` · `XSIM-BLOCKED` (X1/X2/X3 shuttle tiers) (branch `claude/feasibility-approval-planning-5gkcam` from `bootstrap-gate` head)** — an **independent-simulator witness** for the consolidated varcap doubler: `xsim_netgen.py` (netlist generator; **reads** the device point from `shuttle_core` constants + `presets/R1-baseline.json`, never hand-entered), `xsim_x0_galvanic.net`, `xsim_from_solver.py` (pure comparison consumer: runs ngspice, parses the binary `.raw`, pulls native values from `shuttle_core`), `xsim_x0_anchor.png`, `xsim_comparison.csv`, verdict in `xsim-findings.md`. **X-1 engine probe PASSES** — `ngspice-42` installed in-environment (egress permitted), runs headless `-b` batches and emits parseable `.raw` (not an `XSIM-BLOCKED-ENVIRONMENT`). Netlist primitives verified: the **charge-defined time-varying cap** `Cxxx n+ n- Q='C(θ(t))·V'` carries the parametric **V·dC/dt** pump term natively (ngspice `ddt()` B-source is unreliable → use `Q=`); spark gaps as `.model D`/`SW`. **X0 degenerate galvanic anchor RECOVERED:** ngspice **z = 1.2042** vs native 1.2033, Δ = **+0.0009** (inside even the stretch ≤0.002), insensitive to diode ideality. **[IR] named note:** a ngspice voltage-controlled `SW` is **bidirectional** when closed and cannot rectify (z = 1.0, no pump) — the faithful one-way element is a near-ideal `.model D` diode, whose residual Vf does **not** pull z under 1.2033 (measured above); deviates from the brief's "near-ideal switch" suggestion, surfaced not hidden. **X1/X2/X3 shuttle tiers XSIM-BLOCKED (named):** across ~8 principled netlist variants the flying-bucket shuttle (isolated-island Cx-collapse boost + emergent strike via **charge-defined behavioral caps through gated one-way gaps**) either trips ngspice **timestep collapse at the load-station cap↔gap loop** (stiff behavioral-Q-cap internal node) or **fails to ratchet** (rail decays, z≈0.985) — the quasi-static discrete-event protocol has no faithful continuous-time analogue here. The full §5 table is reported with native targets (z_ideal 1.18938, emergent δ 0.2175, six event angles, island ledger ~5e-14, z_arc mid 1.18441, clamp ~1.04×, V_floor 187 V / V_sustain 437 V) standing as unwitnessed targets for a refined-netlist / TMD-side re-run (XSPICE event-driven gaps or fixed-C islands with scheduled injection). Anchor-chain: ngspice and native **agree at the degenerate limit** (not `XSIM-DIVERGENT` — the shuttle quantities are blocked, not mismatched). `reference/doubler_core.py`, `shuttle_core.py`, `index.html` untouched; not merged.
- **bootstrap-gate Phase-5 → `BOOT-SEEDED` (branch `bootstrap-gate` from `spark-derate` head)** — `shuttle_core.py` **extended** with the low-voltage startup tier (new `pTauLeakStorage`/`pLagStat`/`pPriming`/`boot_vfloor`, all default OFF ⇒ spark/ideal paths byte-identical; `_is_ideal` tripwire updated) + `bootstrap_from_solver.py` (pure consumer) + figures (`bootstrap_threshold_map.png`, `bootstrap_trajectory_atlas.png`, `bootstrap_capture_window.png`) + `bootstrap_seeder_spec.csv`, verdict in `bootstrap-findings.md`. **B0 gate PASSES** (B0a spark suite exact; B0b high-V limit reproduces spark z within **3.6×10⁻⁷**, all corners — the low-V model reduces to the validated tier). The machine starts at zero charge (R0): startup is governed by two thresholds + a retention race. **Two-threshold structure exhibited:** **V_floor ≈ 187 V (seed) < V_sustain ≈ 437 V (seed, full speed)**; the gap Paschen minimum **V_FLOOR=327 V** (cited Kuffel, air) is enforced (no conduction below it — the rising branch, B1a), and the varicap squeeze lifts a sub-floor seed to the gap threshold (mapped, not assumed). V_sustain **rises as rpm falls** (the retention race: growth needs `ln(z) > 1/(f_cycle·τ_storage)`), with **no growth below ~660 rpm** (mid). **Seeder spec (deliverable):** a one-shot **≈437 V / ≈43.7 nC** injection onto **node 1 (Ca)** captures ≥99% over ≥20 jitter seeds at the mid corner (B3a); nodes 1/4 vs 2/3 symmetric. **Retention floor:** opt 100 rpm, mid 662 rpm, **pess none ≤3000 rpm** (surface leakage τ≈0.01 s outruns the gain — pess needs operating rpm first or priming, reported not hidden). **B2** spin-up: standstill injection loses the race (fire-and-decay), intermediate/full-speed capture (B2a determinism ≥10 seeds). **B4a conservation** via the itemised arc-energy ledger (the arc drop breaks `load_in==fire_out` by design) + finiteness on decayed-out runs. Startup is arc-mode by necessity (seeds below glow ignition). Named outcomes (`no_fire`/`decayed_out`) counted. The spark-derate pess strike (150 V, sub-Paschen) is corrected to V_FLOOR on the bootstrap path only (B0a/B0b untouched). `reference/doubler_core.py` untouched (mirror FAITHFUL); `index.html` untouched; not merged.
- **spark-derate Phase-3 → four verdicts: `LOADRETURN-CONDITIONAL` · `BACKSTOP-CLEAN` · `SPARK-INDETERMINATE` · `GLOW-INDETERMINATE` (branch `spark-derate` from `shuttle-fullsim` head)** — `shuttle_core.py` **extended** under regression discipline (new `mode='ideal'|'arc'|'glow'` + spark params, all defaulting to ideal; `assert_ideal_identity()` + `_is_ideal()` tripwire), `spark_derate_from_solver.py` (pure consumer) + figures (`spark_derate_z_vs_rpm.png`, `spark_derate_band_map.png`, `spark_derate_load_return.png`) + tables (`spark_backstop_table.csv`, `spark_audit_table.csv`), verdicts in `spark-derate-findings.md`. **C0 regression gate PASSES exactly** (T0a anchor 1.2033; T0b z_shuttle 1.18938 + 6 event angles within 1e-3; T0c ledger 4.6e-14) — rev-0.3 ideal tier reproduced byte-for-byte. The earlier resistive island→target **bleed is removed as physically unrealizable** (island and counter-plate are in relative motion) and replaced by a **backstop second spark-gap boss** (later station, lower threshold `pVbkBackstop`, stray `pCboss2` always in the C_min sum). **LOADRETURN-CONDITIONAL:** trapped charge returns to source only at the pessimistic (lowest-strike) corner; persists at opt/mid; T1 deterministic across ≥10 seeds. **BACKSTOP-CLEAN:** zero false positives (≥500 healthy × ≥10 seeds × 3 corners), catches every uncleared miss, island bounded ≤1.05× single-bucket (unbounded without), boost tax 17.65→16.22 (pCboss2=6 ≥ floor), ordering margin robust (T2a–T2e pass). **SPARK-INDETERMINATE:** z_spark≈1.189/1.185/1.169 (clean gain≈ideal), island overvoltage clamp gap-set at ≈1.04×Paschen strike (invariant to backstop/misfire), arc recovery-failure misfire roll-off for pess from ~4 k rpm (T3a <1% below edge), energy ledger itemised (T3b) — but the self-excitation band `ln(z)·f_cycle > 1/τ_tank` is **empty at the realistic copper-upper-bound Q≤1000** within rpm≤30 k (the 326 kHz tank rings down faster than the modest gain self-excites), opening only at optimistic Q≥2500 (reported as such, never confirmed). **GLOW-INDETERMINATE:** glow ignites/transfers (completion 0.998/0.994/0.985, T4b/T4c pass) but pess completion <99% (T4a fails) and the band is the same optimistic-only. Absolute scale cited verbatim (g=0.5 mm, V_HV=20 kV, f₀=326 kHz, L_RES=123 µH, Q≤1000; the 238 kHz f₀ is resolved/superseded, not a contradiction → no BLOCKER). Physics corners literature-anchored (Kuffel; J.C. Martin). Misfires/constrictions/backstop conductions are named, counted events. `reference/doubler_core.py` untouched (mirror FAITHFUL); `index.html` untouched; not merged.
- **shuttle-fullsim Phase-2 full simulation → `SHUTTLE-PUMP-CONFIRMED` (branch `shuttle-fullsim`, rev 0.3)** — `shuttle_core.py` (new producer; consumes only the frozen device-point scalars + `solve_doubler4`/`ANCHORS`/`run_self_test`), `shuttle_timing_from_solver.py` + `shuttle_timing_from_solver.png` + `shuttle_event_angles.csv`, verdict in `shuttle-fullsim-findings.md`. Direction restored to the frozen **1→3 / 4→2** (rev 0.3). **Anchor PASS** (galvanic degenerate limit z = **1.2033** = `ANCHORS["device"]`, asserted before any campaign step). The flying-bucket shuttle **pumps**: z_shuttle ≈ **1.19–1.20 > 1**, 8 cycles to steady, island charge ledgers balanced to **~10⁻¹⁴** (hard-fail clear, no secular drift), per-branch direction matches the reused `d3_duty_sign_events.csv` (`Q_D3 > 0` into node 3). Bucket budget **26/35 feasible cells**, z → 1.2033 ceiling monotonically as boost grows. `pVbkFire` fire band **[0, ~15]** with δ monotone (0.218→0.390 sector) and `SG3a < SG3b`; outside the band the strike never reaches threshold → island charge accumulation (the `commutator-design.md` C5 failure). Deliberate **drain-back** case (`extend_into_collapse`) degrades the pump 1.189 → **1.087** with ledgers still balanced — failure mode demonstrable, not absent. Timing diagram is a pure consumer (3 θ-aligned panels; six event stations at simulated angles; emergent **SG1 leads SG3b by Δθ ≈ 0.218 sector**); event-angle CSV exported for the phase-plate DXF. `L_RES` = L1 ≈ 123 µH cited (5–6 ring, not the pump). `reference/doubler_core.py` untouched (mirror self-test still FAITHFUL, device z = 1.2033); `index.html` untouched; not merged.
- **shuttle-fullsim Phase-2 gate report (branch `shuttle-fullsim`)** — `shuttle-fullsim-findings.md`. Rev 0.1 halted both pre-build gates (T0: SG1 conducts at C1 *max*; export BLOCKER: no `C(θ)`/`LR` in frozen core). Rev 0.2 resolved those and the `LR` value is now **recorded** — `L_RES` (= L1) ≈ **123 µH** added to `docs/commutator-design.md` §2 (TMD-authorised; origin Block R `resonatorCore`). **New HALT before build:** rev 0.2's "shuttle direction reversed" (load from stack 3/2, fire into terminals 1/4 ⇒ charge **3→1 / 2→4**) is the reverse of the frozen regenerative cross-couple (D3:1→3, D4:4→2). An independent 4-node check shows the frozen direction reproduces the anchor (**z = 1.2033**) while the reversed direction **does not pump (z = 1.0)** — so it cannot pass the mandatory degenerate-limit anchor (`z = 1.203 ± 0.03`). The T0 finding is a *timing* re-anchor only; the charge *direction* stays 1→3 / 4→2 (the 1→3 ratchet the shuttle exists to carry). Direction confirmation returned to TMD; no `shuttle_core.py` / campaign / timing diagram built. `doubler_core.py` and `index.html` untouched; not merged.

### Added
- **Commutator render (commutator v0.1)** — render/consumer visualisation of the physical switching design in `docs/commutator-design.md`; `solveDoubler4` untouched (frozen primitives byte-identical).
  - **Axial companion view (B)** — new "Commutator — axial view" panel (face-on polar canvas): rotor disc + `Nsec` sectors, the 6 floating bars at the transfer radius with ball-electrode ends, the SG3 / SG4 stator sets with the **30° offset drawn**, firing windows + favourable-half shading, and a live **`kphi`** rotor-angle slider that sweeps the bars into the sets. Subset colour/hatch legend.
  - **Front-view additions (A)** — into `drawCrossSection`: mirrored top/bottom bar sets at the transfer radius with ball glyphs, stator electrodes from the leg frame across axial **spark-gap glyphs** (two series gaps per bar), and the cross-ecliptic sink routing for nodes 2/3 kept clear of the C-EM windings (C6); ~75 % opacity so the legend, not line weight, disambiguates.
  - **`drawLegend(ctx, items, x, y)`** helper shared by both views: **colour = electrical frame** (rotor / stator / **floating bars** / rail) via new CSS vars `--rotor/--stator/--floating/--rail`, **hatch = material**.
  - Pure `commutatorGeom(state)` (so geometry is testable without a DOM): bar count = `⌈Nsec/2⌉`, the 30° offset resident in the stator sets (bars axially aligned), transfer-circle radius. New `k*` inputs `ktransrad` / `kbargap` / `kphi` (render-only; no solver coupling). Four contingent self-tests (SG offset = pitch, bar sets aligned, no cross-bridge, transfer radius outboard). Page stamped `commutator v0.1`.
- `docs/commutator-design.md` — switching design record: the physical realisation of the solver's ideal diodes `D1–D4` as a rotary, mechanically-timed electrostatic commutator (hybrid alignment-gated self-break, momentary double-gap bridge, 6 bars/cross-couple, mirrored top/bottom sets, 30° resident in the stator discs). Records the solver-verified conduction pairing `{D1,D3}`/`{D2,D4}`. `solveDoubler4` unchanged; `z = 1.203` remains the ideal-diode ceiling, with `z_spark < z` noted as a derating. Transfer model left open.
- **Block S — firing sequence & clocking (S v0.1)** (`docs/brief-blockS-firing-sequence.md`): a read-only presentational block visualising the spark-gap firing sequence driven by the *actual* doubler solver.
  - **`traceDoubler4`** — a read-only **sibling** of `solveDoubler4` (identical loop, records the per-phase node-voltage trace, reuses the frozen `chargesFromVoltages`/`solvePhase`; carries the running rescale so the consumer reconstructs `Vtrue = V/scale`). The frozen functions (`solveLinear`, `chargesFromVoltages`, `solvePhase`, `solveDoubler4`, `zSym`) are **byte-identical** — a self-test asserts `traceDoubler4.z ≡ solveDoubler4.z` within 1e-9.
  - **Four canvas panels** ported from the reference `.py` prototypes: `seq-v` (gap voltages |V1−V3|/|V4−V2| with the convex 1/C within-stroke rise, peaks growing ×z/cycle), `seq-logic` (SG3/SG2/SG4/SG1 conduction with stroke bands), `seq-tank` (impulse-kicked damped 5–6 ring whose envelope grows with the pump, plus a resolved-carrier inset), and `seq-clock` (polar map: `groups = ⌈Nsec/2⌉`, `pitch = 720/Nsec°`, reacts to geometry `pnsec`).
  - **Single timing source** `machinePRF()` (`⌈pnsec/2⌉·rrpm/60`); a self-test asserts it equals `resonatorCore.prf` and `demMotor.prf` so the three can't diverge. Tank f₀/Q **consume Block R** by default (`sf0`/`sq0` = 0 ⇒ auto) — no hard-coded carrier.
  - New `s*` inputs (`snshow`, `sf0`, `sq0`, `svfire` reserved, `sfollow` toggle), CSS vars `--sg3/--sg2/--sg4/--sg1/--res`, reference `.py` prototypes in `reference/`. Five self-tests (tracer≡frozen, z-growth wired, tank monotone, clocking geometry, PRF single-source guard). Read-only sink; `solveDoubler4` untouched. Page stamped `S v0.1`.
- **Cross-section render upgrade (xsec v0.2 → v0.2.2).** Extends `drawCrossSection` only — no solver/producer edits.
  - **Motor fix (v0.2.2).** The §5e pole + C-EM are now derived from the rim band `[rActiveOuter, rRotorOuter]` via a pure, mirror-symmetric helper `cemMotorGeom(rs,…)`, fixing three faults: (1) the pole no longer overflows the band and "eats the rotor" — it is a band-sized square that sits on the disc; (2) the winding moved to the **vertical spine** (back-iron), clear of the disc, with per-turn hatches; (3) the two ±x C-EMs are exact mirrors (all coordinates `X(rs·radius)`), and the spine stands 6 mm outboard of the disc rim so nothing crosses the rotor disc. `demQuadConeRmm` no longer sizes the pole (fork F-X1). Six new self-tests (mirror symmetry, no-inboard-overrun, spine clears disc, pole fills band, two equal axial gaps, coil-on-spine). Page stamped `xsec v0.2.2`.
  - **Ca/Cb bracket standoff.** Step 5d now lifts the bus-ring stack off the stator back by a real, tunable **axial** height `tcBracketMm` (default 70 mm, range 0–200) instead of a fixed ~8 px; the legs read as the standoff bracket rising toward the cone apex. Soft warn (Block-T panel) when the standoff exceeds the cone-apex height.
  - **Reluctance motor in the meridional cut (new step 5e).** Draws the 2-of-6 rotor steel poles and 2-of-12 stator C-EMs the section plane passes through. The pole is an **upright square** (`--cem-core`); the C-EM is a **C clamped over the rotor rim** (`--cem-core` arms/spine + `--cem-coil` windings) whose two pole faces **straddle the square pole across two equal axial gaps** — the picture-level statement of `demNGaps = 2` — opening toward the axis, spine on the rim. Both gaps are sourced from Block-D `demClearanceMm`. Honest 2-each idealisation; pole on rotor, C-EM on stator, coupled only by the gaps. Optional N/S jaw labels behind `dlabels`. *(§3 geometry corrected from the v0.1 diamond/single-mouth U-core.)*
  - **De-collided legend.** New `--coil` (warm gold) for the resonator coil so it no longer shares `--copper` with the Ca/Cb stack; the cross-section legend is now data-driven (`XSEC_LEGEND`), always visible, and names every drawn part once.
  - Four render self-tests (bracket px→mm round-trip, pole-in-band + band width, motor gap sourced from Block D, legend coverage). Confirmed the coil draws (`updateResonator` runs before `drawCrossSection`).
  - **[IR] reconciliation:** the brief defines `tcBracketMm` as the axial standoff, but Block-T v0.1 already used that id for the **radial** inner-pin clearance. Renamed the radial input to **`tcInnerPinMm`** (same default 15 mm) to free `tcBracketMm` for the axial meaning, per the brief's "do not conflate" instruction. Page stamped `xsec v0.2`.
- **Design-flow inheritance + parameter-set presets (flow+presets v0.2).** Folded in the five-coupling design-flow inheritance and added a JSON preset loader/saver.
  - **Five default-on / overridable couplings**, applied in one acyclic `cascadeState` pass before any producer runs: `vhvLink` (design HV → HV void = `vhvKV/vhvEkVmm`, Block-T design V, motor bias), `quadLink` (`pquadfoot = 2·demQuadConeRmm`), `demRpmLink` (motor RPM ← master `rrpm`), `demEvLink` (`demEventsPerRev = ⌈pnsec/2⌉`, making the R and D PRF identical by construction), `tcFromCmax` (`Ca = Cb = tcRatio·Cmax` via Block-T inverse, driving the solver). Slaved inputs render disabled-with-value while their link is on. New design primaries `vhvKV`, `vhvEkVmm`, `demQuadConeRmm`, `tcRatio`.
  - **Presets** = JSON of primaries + toggles + an `expect` block. "Load parameter set" reads a self-documenting file (`FileReader`), validates (unknown/out-of-range keys warned + skipped, never applied silently), applies, runs the cascade, **checks each `expect` within tolerance**, updates the URL hash, and shows a notes panel (title/description/applied/overridden/expect ✓✗). "Save current as preset" exports a scaffold (primaries + toggles + current outputs as `expect` stubs). Local file I/O only — **no network, no `localStorage`**.
  - **Magnitudes now live in presets' `expect` blocks, not in prose** — `presetExpectGetters` defines the checkable outputs (`CmaxNF`, `CaNF`, `prfHz`, `pvoidMm`, `f0kHz`). This retires the stale-number problem (e.g. f0) entirely.
  - Worked **R1 baseline** shipped both embedded (for the self-test) and as `presets/R1-baseline.json`. Eight new self-tests (round-trip idempotence, partial load, R1 expect-pass, corrupt-expect-surfaces-✗, inheritance-overwrite warn, unknown-key warn, bad-JSON safety, flow identities + idempotent cascade).
  - **[IR] reconciliation:** `demCapRatingKV` default raised 20 → 30 kV so the default cascade (`demBiasKV ← vhvKV = 20`) lands with real AC headroom instead of a zero-headroom Block-D warning. The classic electrical presets (device/wide/narrow/no-swing) set `tcFromCmax:false` so their typed Ca/Cb survive the cascade. Page stamped `flow+presets v0.2`. `solveDoubler4` untouched.
- **Block T v0.1 — built-in transfer caps Ca / Cb** (`docs/brief-blockT-transfer-caps-v01.md`): a producer that designs the physical transfer caps as a **solid annular bus ring** on legs over the stator sectors + Mylar + upper electrode = Ca (top face) / Cb (bottom face).
  - **Bus ring is a full annulus (no keptFrac)** — it buses the otherwise-unconnected stator sectors *and* is the lower Ca/Cb plate (node 1 ↔ 2; mirrored 4 ↔ 3). `transferCaps` consumes `rActiveInner`/`rActiveOuter` from `plateGeom` (Block C-I). **[OC]**
  - **Inverse** (desired Ca → ring width) and **forward** (width → Ca) modes; **inside placement default** (inner edge pinned at `rActiveInner + bracket`, grows outward to clear the rim quadricones, must stay ≤ `rActiveOuter`), **outside** toggle (outer edge pinned at the leg radius, grows inward). Round-trip exact. Worked point: 7 nF → ≈191 mm width at 1 mm Mylar (εr 3.2).
  - **Outputs:** ring width, inner/outer radii, plate area, realised Ca = Cb, band-max Ca, dielectric field (kV/mm), and ½·C·V² energy. **Five warnings:** band overrun (clamps the drawn ring), Ca above band-max, thin-bracket live-clearance, high-field (>40 warn / >100 bad), and the solid-annulus note.
  - **§5d cross-section render:** the copper–Mylar–copper sandwich draws on the **back of each stator plate** in the Block-M axial view (legs → bus ring → Mylar → upper electrode), radial span and Mylar thickness tracking the real producer values; `Ca`/`Cb` labelled with the dimensions overlay; legend swatches added.
  - **Solver hand-off (no `solveDoubler4` edit):** a `tcDrive` toggle routes the realised Ca = Cb into the solver's `ca`/`cb` state at the call site only, raising their field max (Block T is nF-scale, far above the manual 500 pF range) and disabling manual entry — mirroring the rotor-cap raise-max policy.
  - New `tc*` namespace (CONVENTIONS §4), seven self-tests (inverse widths 129/191/258 mm, round-trip, band-max ≈12.3 nF + overrun, Ca=Cb symmetry, field, inside>outside width, 1.4 J energy). Page stamped `T v0.1`.

### Changed
- **Block C-I v0.2 — active-overlap squeeze + C_R decoupling.** `plateGeom` now returns **two** areas: `Ametal_full` (the full rotor face) **and** `Ametal_active` (the swinging rotor↔stator overlap band `[ro+void, plateR−bus−(quadfoot+quadclr)]`). The counter-rotating stator can't reach the central HV tank (inner `pvoid`) nor overlap the 6 rim steel cores (outer `pquadfoot + pquadclr`), so the pump area is squeezed inward at both ends.
  - **`plateCaps` uses the active area** → pump `Cmax` shrinks ≈ ×0.58 at defaults (94 mm steel-core band + 10 mm clearance + 9 mm bus + 20 mm inner void; `A_active ≈ 0.221 m²` vs `A_full ≈ 0.384 m²`).
  - **`resonatorCore` uses `Ametal_full` for `C_R`** — `C_R` is rotor↔rotor through the mica disc, so it's decoupled from the stator clearances and `C_R`/`f0` stay unchanged (verified by a C_R-invariance self-test). **[OC]**
  - New `p`-inputs (mm): `pvoid`, `pbus`, `pquadfoot`, `pquadclr`. New readouts: `A_full`, `A_active`, active band (mm), squeeze ratio. Three warnings: band-collapsed (hard), HV-void floor from the Block-D bias at 1 kV/mm (derated), and the standing decoupling note. Five self-tests (squeeze applied, band radii, **C_R invariance**, pump ∝ active area, collapse guard). Page stamped `C-I v0.2`. `solveDoubler4` untouched.

### Added
- **Block D — Distributed electromagnets (reluctance spin-up motor)** (`docs/brief-blockD-distributed-electromagnets.md`): a fourth independent producer (never writes the rotor caps, never calls `solveDoubler4`) modelling the 12-C-EM stator reluctance motor that spins the machine up.
  - **Iron-rotor reluctance model:** 12 stator C-electromagnets in two interleaved groups of 6 (odd → group A on transfer cap C3, even → group B on C4), uniform winding. The N-S-N-S ring pattern is a *consequence* of the two transfer caps swinging antiphase (push-pull); a parity self-test asserts the A↔C3 / B↔C4 grouping and the adjacent-pole alternation (the assumption the whole map rests on). **[OC]**
  - **Series resonant DC-block cap** (default 440 nF / 20 kV): blocks the kV DC bias, passes the low-frequency stepping drive at minimum impedance (≈R_coil), and stays a high-Z spectator at the central f0 tank — one component doing the DC block, torque tuning, and f0 isolation.
  - **Q / turns / voltage budget** (`demMotor`): resonant `L_coil = 1/((2π·f)²·C)`, `Z0 = √(L/C)`, gapped turns `N = √(L·l_gap/(μ0·A))`, coil R/Q from AWG (reuses `awgToCond`, `MU0`, `RHO_CU`), ripple ceiling `(V_rating−V_bias)/Q`, circulating current `(V_rating−V_bias)/Z0`, peak cap voltage `V_bias + Q·V_ripple`, and the **closed-form ampere-turn limit** `N·I = (V_rating−V_bias)·√(C·l_gap/(μ0·A))` — frequency- and turns-independent (adding turns raises L and Z0 so I drops to compensate). Validated against the brief §4.5 worked example (150 Hz: L≈2.56 H, Z0≈2411 Ω, N≈3190, N·I≈10 585 A-t; 300 Hz: same N·I at half the turns). **[OC]**
  - **Drive-frequency pin (4× swing):** `PRF = events/rev·rpm/60`; alt-stroke (push-pull) ⇒ coil runs at PRF/2, per-stroke ⇒ PRF — a `demDriveMode` toggle, since it sets `L` (and N) by 4×.
  - **Per-coil (×12) vs per-group (×2) caps** toggle (`demCapTopology`): same total stored energy (≈1.06 kJ at 20 kV — lethal HV on the spinning stator), reported per-cap and total.
  - **Over-voltage warnings:** `V_cap,peak > rating`, ripple over ceiling, bias ≥ rating (no torque), plus the standing antiphase-push-pull reminder.
  - **Top-view projection panel** (user-directed, supersedes the brief's §8 side-ring): an axial projection showing the sectored electrode disc (from `pnsec`), the 6 salient rotor iron poles, and the 12 stator C-EMs (steel core + copper coil, group A/B tint, N-S-N-S polarity chips at the C3⁺ instant), with an optional angle/index label overlay and a dedicated legend (`--cem-coil/--cem-core/--cem-a/--cem-b`). Preset `spin-up` at the brief's worked design point.
  - **Seven self-tests** (brief §7): resonance round-trip, Z0 identity, ampere-turn invariance (PRF vs PRF/2, from separate N and I), over-voltage flag, 88 J energy, N-S-N-S antiphase parity, per-group cap = 6·C. New `dem*` namespace documented in `CONVENTIONS.md` §4. Parallel producer; the solver is untouched.
- **Per-parameter inline help.** A single "? param help" toggle in the header (off by default — zero added clutter) reveals a one-line description under every parameter across all four panels (40 descriptions from one `DESC` map; numeric rows + non-numeric controls). Native `title` tooltips are also set on every label. The toggle state round-trips in the URL hash (`help`).
- **Block R — Central resonator** (`docs/brief-blockR-central-resonator.md`): an independent producer (never writes the rotor caps, never calls `solveDoubler4`) modelling the `C_R ∥ L` tank formed by the two conical coils (series, aiding) and the through-mica inter-electrode capacitor.
  - **Inductance** via an exact conical loop-stack: HF self-inductance + Maxwell mutual inductance over all turn pairs, with complete elliptic integrals K/E by the **AGM** (no library); decimation to ≤400 nodes for very fine wire (flagged).
  - **Capacitance** `C_R = ε0·εr_mica·A_align/discH` (electrodes fully aligned — locked premise), a Medhurst self-capacitance estimate, and the coil self-resonant frequency.
  - **LC + loss:** `f0, f_d, Z0, Q (copper-only upper bound), τ, R_dc/R_ac, skin δ`; conductor as **wire (AWG) / tube / manual-OD**, with the verified capillary-tube ≈ solid-rod HF identity (equal f0 & Q at less copper).
  - **Drive from RPM:** `prf = ⌈Nsec/2⌉·rpm/60`, ring cycles per pulse, `settle = τ·prf` and the isolated-ringdown vs build-up regime.
  - Adds the coil to the cross-section (copper band hugging each cone slant, per-turn hatch, tube/wire hint, guard-coloured) and a **colour + hatch legend** (`--copper/--steel/--diel`) toggled with the dimensions overlay; swatches tie readouts to drawing regions. Preset `resonator`. Six self-tests (AGM, C_R, validated L, tube≡rod, AWG, PRF).
  - **Honest correction:** the brief's quoted `L ≈ 235 µH / f0 ≈ 238 kHz` came from a cruder cylindrical estimate; the validated conical loop-stack (cross-checked against Nagaoka) gives `L ≈ 131 µH / f0 ≈ 316 kHz`. The validated value is used and the self-test asserts it. **[corrected openly]**

### Corrected
- **C-I sectored-metal area** now uses the annulus *outside the ring* (`ring-out → plate`) instead of the full disc, removing a ~2 % overcount (the inner disc is the ring/hub, not free sectors). Slightly lowers `Cmax` (default 7237 → 7080 pF; `Cmin` unchanged) and feeds the corrected `A_align` to Block R. The C-I fixed-geometry self-test (ring off) is unchanged. **[review finding]**

### Changed
- **Rotor caps now inherited from the plate-geometry capacitance by default** (`psrc:"plate"`): `C1/C2 min/max` are driven by the Block C-I `Cmin/Cmax` (both rotors via `plink`, the symmetric design) and their inputs are disabled. Only **transfer + stray (Ca/Cb/Cpar)** stay user input (can't be estimated yet). The manual override toggle remains; the Rotor-caps panel shows an "← inherited from plate geometry" tag.

### Added
- **Stator plates in the Block M cross-section** — a fixed stator plate above and below the rotor, each separated from the rotor electrode by the capacitor gap `g` (C-I `pgap`), with **stator ⌀ = rotor/plate ⌀** and a central clearance at the hub for the cone. Drawn as annular bands (`--edge`) with a dashed gap leader; labelled (stator ⌀ · gap g) when dimensions are on. The gap is drawn to scale but clamped to ≥2 px for legibility. **[IR render]**

### Changed
- **Blocks C-I ↔ M now geometrically coupled** (user-directed; supersedes the briefs' warn-only "do not hard-link" stance, recorded here per the *correct openly* convention):
  - **Plate ⌀ (`pdia`) → disc ⌀ (`mdiscdia`)** — the outer (electrical) diameter family drives the structural disc.
  - **Ring-outer ⌀ (`prouter`) → quadricone/hub ⌀** — `hubDia = prouter`, `coneR = hubDia/2`. The hub is no longer *derived* from key sizing; instead the DIN key/void/shaft assembly becomes a **boundary condition** checked against the ring-set hub (new hard guard *"hub fits ring (key sizing ≤ ring ⌀)"*).
  - Sectors (`pnsec`) remain the primary structural input in C-I (12 → 6 kept/6 gap; 8 → 4/4) and are surfaced in the M panel.
  - Coupling is a toggle (`couple`, default **on**, hashed as `cpl`); when on, the M disc-⌀ input is disabled and tracks the plate ⌀. Standalone mode (and the Block M self-tests, which exercise the key-sizing hub) are unchanged.
  - Defaults retuned to a coherent coupled landing device (1 m plate/disc, 12 sectors, ring/hub ⌀ 150 mm, void 40 / shaft 20 / disc-thick 10 mm) so all guards land green; `rotor-core` preset rebuilt as the coupled showcase, `plate-air`/`plate-mica` set `couple:false`.
  - Ring diameter field maxes raised 50 → 100 cm to allow larger hubs.

### Added
- **Block M — Rotor mechanical core** implemented in `index.html` as a second, independent producer (never writes the rotor caps, never calls `solveDoubler4`; brief §6.4):
  - `docs/brief-blockM-rotor-mechanical-core.md` — the implementation brief.
  - Quadricone + stub-shaft + spherical-void + dielectric-disc geometry ported to vanilla JS (`quadriconeCore`): hub from key sizing (`hubDia = voidDia + shaftDia + 2·keyLen − discH = 2·coneR`), spherical caps + septum belt + bore volumes, and the four hard guards (collar / void-seat / wall / key-fit) + keyway-collar soft guard with a binding-guard readout. **[OC]**
  - DIN 6885-1 key sizing: `keyLenFor` (≈1.5·D snapped up the standard-length ladder) and a key cross-section table (`keySectionFor`) for the keyway/`t₂`. **[OC]**
  - HV-geometry distances reported (`clearN`, `clearEE`, `creepEE`) — geometry only, no safe-voltage assertion (deferred). **[OC]**
  - Inputs `mvoid mdiscvoid mdisch mshaft mwall mdiscdia` added to `FIELDS` (mm); `munit/mfit/mdims` hashed manually. `hubDia` is derived, not an input.
  - **Live axial cross-section** (`drawCrossSection`, canvas, host idiom): isotropic auto-fit with `fit | lock-scale` toggle, mm scale bar + px/mm, toggle-able dimension annotations, and guard-coloured features. Hooked into `drawCharts()`.
  - Warn-only coupling vs the C-I plate (`plateDia` vs `discDia`, hub vs electrode annulus) — panels are deliberately not hard-linked (§4 / open fork §7.1).
  - Preset `rotor-core` (15 cm-class worked example, `hubDia = 150 mm`), leaving electrical/C-I fields untouched.
  - Self-tests added to `runSelfTest()` (§6.6): `hubDia = 197 mm`, void-partition identity (`2·cap + belt = sphere`), and `keyLenFor(35) → DIN 56` — all passing.
  - **[IR] correction:** the brief specified a `cm/in` unit toggle, but `mm` is the realistic base for these component sizes (void 50, disc 1000); Block M works in mm with an `mm | in` toggle. Recorded here per the "correct openly" convention.
- **Block C-I** implemented in `index.html` — *"Rotor plate — geometry → capacitance"* panel (producer/consumer pattern; `solveDoubler4` untouched):
  - Ported the sectored-disc + ring area math from `reference/SectoredDiscCalculator.jsx` to vanilla JS (`plateGeom`, SI; kept fraction = `ceil(Nsec/2)/Nsec`).
  - Capacitance model `C = ε₀·εr·A/g` with rotation extremes `Cmax`/`Cmin` and the ring as the `Cmin` floor (`plateCaps`); azimuthally-symmetric ring ⇒ rotation-independent floor. **[OC/IR]**
  - Dielectric model: Vacuum (exact 1), live moist-air (`epsAir` — Smith–Weintraub refractivity + Buck 1981 saturation pressure), and fixed-nominal Kapton/Mica with tolerance bands. Env inputs shown only for Air. **[OC; IR for solids]**
  - Plate inputs `pnsec pdia prouter prinner pgap ptempc ppatm prh` added to `FIELDS` (hash-serialise for free); non-numeric controls `pdiel pring plink psrc punit` serialised manually in `writeHash`/`loadFromHash`.
  - Producer pre-step at top of `recompute()`: in `psrc:"plate"` mode geometry drives `c1{min,max}` (and `c2` when `plink`), the derived inputs are disabled, and the rotor-field max is raised to 10000 pF (**raise-max** field-range policy, brief §6.5/§7.1). **[IR]**
  - Warnings: ring inner ≥ outer, ring outer > plate ⌀, `gap ≤ 0`, fringing heuristic (`gap ≳ 10%` of smallest in-plane feature), ring-off (`Cmin=0`, κ_C→∞), `Cmax` clamped, solid-dielectric-in-rotary caveat (§3.3).
  - Presets `plate-air`, `plate-mica` (set `psrc:"plate"`); existing electrical presets now imply `psrc:"manual"`.
  - Plate self-tests added to `runSelfTest()` (brief §6.7): dry-air εr ≈ 1.000576 and fixed-geometry `Cmax` ≈ 347.7 pF — both passing; engine badge stays "engine verified".
- Project scaffolding: `README.md`, `CONVENTIONS.md`, `CLAUDE.md`, `.gitignore`.
- `docs/brief-blockC1-geometry-to-rotorcap.md` — **Block C-I** implementation brief (geometry → rotor-cap front end). Consolidated from two prior drafts:
  - *draft 0.1* — capacitor model (parallel-plate with rotation-dependent overlap), dielectric presets (mica / Kapton / air / vacuum) incl. the moist-air refractivity model, and the symbol-hygiene convention. Targeted a **standalone React extension** of the area engine.
  - *draft 0.2* — **retargeted to integration** into the existing `index.html` doubler in its vanilla-JS idiom; established the producer/consumer pattern (`solveDoubler4` left untouched); added the rotary-varcap dielectric-practicality note (§3.3) and the host-field-id mapping (§1.1).
- `reference/SectoredDiscCalculator.jsx` — parametric area engine (sectored disc + ring; alternating kept sectors, central ring, rotation-overlap). Source for the vanilla-JS port.
- `index.html` — Symmetric Bennet Doubler 4-node pump-action simulator (host; provided, working, self-tested).

### Pending
- Deferred open forks (brief §7): θ-sweep C-vs-θ visualisation; gating solids behind a fixed-gap sub-mode; dielectric-strength → safe-voltage bound and leakage loss (the genuine next block); fringing correction for non-small `gap`.

### Notes
- Adopted **flat filenames + this changelog** as the audit trail, replacing the earlier `_vNN`-suffixed brief files now that the project lives under git.
